# 拾忆 · 项目工作规则（RULES）

本文件是常驻规则。每轮开工前先读这里；与本文冲突的默认习惯以本文为准。
不改动世界书、聊天记录、总结、预设、API 配置，不控制用户电脑，不要求用户盲测。

## R001 — 画草图就直接画

用户说"画草图 / 给我看看 / 草图呢"时，**唯一要交付的就是草图本身**：

- 直接产出可打开的 HTML + 渲染好的截图，一屏能看完全貌。
- 不要长篇解释、不要罗列"要你定的 N 件事"、不要反问细节。
- 不要在草图后面附大段设计说明；最多留一行小字标注疑问点。
- 草图的文字要**最直白**：短标签、数字、按钮；不写 AI 式的说明文和免责句。
- 用户会自己看、自己指着问；**不要替用户先把所有设计问题问一遍**。

来源：用户 2026-09-17 原话"我要的是画草图给我看，最直白最直接，记录进 RULES"。

## R002 — 少步骤：点了就执行

不要"预览 → 确认 → 再开始"这种繁文缛节。用户点「立即总结」「立即更新」就应该真的开始。

- 不给用户看内部拆批、内部片段、内部编号。
- 日志不要一堆分批展示：一行说清（楼号范围 + 模型 + 可读错误），细节折叠或按需「只看失败」。

## R003 — 页面只负责"看和改"

- 展示类页面（记忆、人物、召回）不放开关注 / 开始任务的设置。
- 更新的开关、周期、进度、失败与修复，全部集中到 **设置 → 更新中心**。
- 界面里不写解释性废话；数字、状态、按钮优先。用户看得懂 `#1–70 ｜ 下次 #71`，不需要旁白。

## R004 — 报告与交付

- 失败要**说真话**：没落盘就说没落盘，不许把"已派发"写成"已保存"。
- 报告里必须区分"隔离模拟验收"和"实体 Android 未测试"。
- 每个结论要能指到具体代码或日志；拿不到证据就写"未确认"，不猜。

## R005 — 当前迭代目标（2026-09-17）

UI 重构，用户逐模块验收。先做记忆模块，顺序：记忆 → 人物 → 更新中心 → 设置。召回本次不动；助手入口先隐藏。

## 环境与边界（备忘）

- 本机只有 Windows PowerShell 5.1，无 pwsh；`test/tt-launcher-reopen.test.js` 相应跳过。
- 本机 git 走本地代理 `127.0.0.1:7897`；代理后端没开时 push 会 schannel 握手失败，属环境问题。
- 发布：`npm test` → `npm run build` → `npm run verify:release` → 与制品绑定的自用回执 → 正常非强推 `publish-release --personal-release --push`。
