import { esc,field } from '../src/product-settings-ui.js';
import { CATEGORY_LABELS,recordDescription,readable,renderMemoryCard,memoryCards } from '../src/product-memory.js';
import { MEMORY_CATEGORIES,pageSummaryBatches } from '../src/product-batches.js';
import { parseModuleField } from '../src/product-custom-modules.js';

export function batchManagementHTML(){return `<h3>总结批次</h3><p class="sy-help">每页 10 批，点击展开详情。删除或重生不会改动聊天原文。</p>
  <div class="sy-batch-filters">${field('查找批次','<input data-batch-search placeholder="#120、100-200、第3批或侧重点">')}${field('状态','<select data-batch-status><option value="active">当前批次</option><option value="saved">已完成</option><option value="failed">失败 / 中断</option><option value="pending">待处理 / 运行中</option><option value="deleted">回收站</option></select>')}${field('每页','<select data-batch-size-list><option>10</option><option>20</option><option>50</option></select>')}</div>
  <div class="sy-actions"><button type="button" data-batch-current>当前批次</button><button type="button" data-batch-recycle>回收站</button></div><p data-batch-total class="sy-help" role="status"></p><div class="sy-actions sy-batch-select"><button type="button" data-select-page>选择本页</button><button type="button" data-select-filter>选择全部筛选结果</button><button type="button" data-clear-selection>取消选择</button></div><p data-selected-count class="sy-help" role="status"></p><div class="sy-actions"><button type="button" data-bulk="regenerate">重生所选</button><button type="button" data-bulk="delete">删除所选</button><button type="button" data-bulk="restore">恢复所选</button></div><div data-batches></div><div class="sy-batch-pagination"><button type="button" data-batch-prev>上一页</button><span data-batch-page></span><button type="button" data-batch-next>下一页</button></div><label class="sy-field sy-page-jump"><span>跳到第几页</span><input type="number" min="1" data-batch-jump value="1"><button type="button" data-batch-go>跳转</button></label>
  <details class="sy-card"><summary>记忆回收站</summary><div data-recycle></div></details>`;}
const statusNames={queued:'待处理',running:'处理中',saved:'已保存',failed:'失败，可重试',interrupted:'中断，可重试',deleted:'已删除'};
export function batchRecordDescription(r,modules=[]){const tag=parseModuleField(r.field??r.key),m=modules.find(m=>m.id===tag?.moduleId),f=m?.fields.find(f=>f.id===tag?.fieldId);return f?`${m.name} · ${r.entity??m.subject} · ${f.label}：${readable(r.to??r.value??r.newValue)}`:recordDescription(r);}
export function mountBatchList({panel,app,run,host}){
  const $=s=>panel.querySelector(s);let currentPage=1,lastPaint='',lastScope='';const selected=new Set();
  const pageOptions=()=>({query:$('[data-batch-search]').value,status:$('[data-batch-status]').value,page:currentPage,pageSize:Number($('[data-batch-size-list]').value)});
  function selectionState(s){$('[data-selected-count]').textContent=`已选 ${selected.size} 批`;for(const button of panel.querySelectorAll('[data-bulk]'))button.disabled=s.busy||!selected.size;for(const button of panel.querySelectorAll('[data-select-page],[data-select-filter]'))button.disabled=s.busy;}
  $('[data-select-page]').addEventListener('click',()=>{for(const b of pageSummaryBatches(app.state.batches,pageOptions()).items)selected.add(b.id);paint(app.state);});
  $('[data-select-filter]').addEventListener('click',()=>{const options={...pageOptions(),pageSize:50};const first=pageSummaryBatches(app.state.batches,options);for(let page=1;page<=first.pages;page++)for(const b of pageSummaryBatches(app.state.batches,{...options,page}).items)selected.add(b.id);paint(app.state);});
  $('[data-clear-selection]').addEventListener('click',()=>{selected.clear();paint(app.state);});
  for(const button of panel.querySelectorAll('[data-bulk]'))button.addEventListener('click',()=>run(async()=>{const ids=[...selected],action=button.dataset.bulk,label={regenerate:'重新生成',delete:'删除',restore:'恢复'}[action];if(!host.confirm?.(`${label}所选 ${ids.length} 批？${action==='regenerate'?'将按楼层顺序调用总结模型，新结果成功后替换旧结果。':action==='delete'?'可在回收站恢复，聊天原文不变。':''}`))return;await app.manageBatches(ids,action);selected.clear();paint(app.state);},{name:button.dataset.bulk==='regenerate'?'focus-summary':'batch-management',button}));
  function move(page){currentPage=page;paint(app.state);$('[data-batch-total]').scrollIntoView({block:'nearest'});}
  $('[data-batch-prev]').addEventListener('click',()=>move(currentPage-1));$('[data-batch-next]').addEventListener('click',()=>move(currentPage+1));$('[data-batch-go]').addEventListener('click',()=>move(Number($('[data-batch-jump]').value)));
  for(const name of ['data-batch-search','data-batch-status','data-batch-size-list'])$(`[${name}]`).addEventListener(name==='data-batch-search'?'input':'change',()=>{currentPage=1;selected.clear();paint(app.state);});
  for(const [selector,status]of [['[data-batch-current]','active'],['[data-batch-recycle]','deleted']])$(selector).addEventListener('click',()=>{currentPage=1;selected.clear();$('[data-batch-search]').value='';$('[data-batch-status]').value=status;paint(app.state);});
  function fill(node,b,s){
    const linked=new Map(memoryCards(b.records??{},{includeAwareness:true}).map(c=>[c.id,c]));const body=node.querySelector('[data-batch-body]');body.innerHTML=`${b.error?`<p class="sy-batch-error">${esc(b.error)}</p>`:''}<p class="sy-help">${b.previousOperation&&b.status!=='saved'?'上次成功结果仍保留。':''}${b.requests?`本批 ${b.requests} 次模型请求。`:''}</p><div class="sy-actions"><button type="button" data-regenerate-batch="${esc(b.id)}" ${s.busy||!Number.isInteger(b.startIndex)?'disabled':''}>${b.status==='saved'?'重新生成':'重试 / 生成'}</button>${b.status==='deleted'?`<button type="button" data-restore-batch="${esc(b.id)}">恢复旧结果</button>`:`<button type="button" data-delete-batch="${esc(b.id)}" ${s.busy?'disabled':''}>删除该批记忆</button>`}</div>${MEMORY_CATEGORIES.map(key=>`<details><summary>${CATEGORY_LABELS[key]} · ${b.counts?.[key]??0} 条</summary>${(b.records?.[key]??[]).map(r=>`<p>${Number.isInteger(r.floorIndex)?`#${r.floorIndex} · `:''}${esc(batchRecordDescription(r,s.modules))}</p>${r.keyDialogues?.length||r.viewpoints?.length?`<div class="sy-packet sy-help">${esc(renderMemoryCard(linked.get(r.id)??r,s.settings,{metadataOnly:true,detail:true}))}</div>`:''}`).join('')||'<p class="sy-help">本批未生成此类记录。</p>'}</details>`).join('')}`;
    body.querySelector('[data-regenerate-batch]')?.addEventListener('click',event=>run(()=>app.regenerateBatch(b.id),{name:'focus-summary',button:event.currentTarget}));
    body.querySelector('[data-delete-batch]')?.addEventListener('click',()=>run(async()=>{if(host.confirm?.('删除该批生成的记忆？聊天原文不变，可恢复或重生。'))await app.deleteBatch(b.id);}));
    body.querySelector('[data-restore-batch]')?.addEventListener('click',()=>run(()=>app.restoreBatch(b.id)));
  }
  function paint(s){
    const scope=JSON.stringify(s.core?.scope);if(scope!==lastScope){lastScope=scope;currentPage=1;lastPaint='';selected.clear();$('[data-batch-search]').value='';$('[data-batch-status]').value='active';}
    const p=pageSummaryBatches(s.batches??[],{query:$('[data-batch-search]').value,status:$('[data-batch-status]').value,page:currentPage,pageSize:Number($('[data-batch-size-list]').value)});currentPage=p.page;
    for(const id of selected)if(!s.batches.some(b=>b.id===id))selected.delete(id);selectionState(s);
    const recycled=$('[data-batch-status]').value==='deleted';$('[data-batch-recycle]').textContent=`回收站（${p.deletedTotal}）`;
    for(const button of panel.querySelectorAll('[data-bulk]'))button.hidden=button.dataset.bulk==='restore'?!recycled:button.dataset.bulk==='delete'&&recycled;
    const signature=JSON.stringify([p,s.busy,s.modules,[...selected]]);if(signature===lastPaint)return;lastPaint=signature;
    const opened=new Set([...panel.querySelectorAll('[data-batch-id][open]')].map(n=>n.dataset.batchId));
    $('[data-batch-total]').textContent=`当前 ${p.currentTotal} 批 · 回收站 ${p.deletedTotal} 批 · 筛选出 ${p.total} 批`;$('[data-batch-page]').textContent=`${p.page} / ${p.pages} 页`;$('[data-batch-jump]').max=String(p.pages);$('[data-batch-jump]').value=String(p.page);$('[data-batch-prev]').disabled=p.page===1;$('[data-batch-next]').disabled=p.page===p.pages;
    $('[data-batches]').innerHTML=p.items.map(b=>`<details class="sy-card sy-batch-row" data-batch-id="${esc(b.id)}" ${opened.has(b.id)?'open':''}><summary><input type="checkbox" data-select-batch="${esc(b.id)}" aria-label="选择第 ${b.number} 批" ${selected.has(b.id)?'checked':''} ${s.busy?'disabled':''}><span class="sy-batch-title">第 ${b.number} 批 · ${Number.isInteger(b.startIndex)?`#${b.startIndex}–${b.endIndex}`:'旧版未保存楼层号'}</span><span class="sy-batch-status" data-state="${esc(b.status)}">${statusNames[b.status]??esc(b.status)}</span></summary><div data-batch-body></div></details>`).join('')||'<p class="sy-empty">没有符合条件的批次。</p>';
    for(const input of panel.querySelectorAll('[data-select-batch]')){input.addEventListener('click',e=>e.stopPropagation());input.addEventListener('change',()=>{input.checked?selected.add(input.dataset.selectBatch):selected.delete(input.dataset.selectBatch);selectionState(app.state);lastPaint='';});}
    for(const node of panel.querySelectorAll('[data-batch-id]')){const b=p.items.find(b=>b.id===node.dataset.batchId);if(node.open)fill(node,b,s);node.addEventListener('toggle',()=>{if(node.open&&!node.querySelector('[data-batch-body]').hasChildNodes())fill(node,b,app.state);});}
  }
  return {paint};
}
