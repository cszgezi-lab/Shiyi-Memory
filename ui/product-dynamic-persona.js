import {esc,field,setting} from '../src/product-settings-ui.js';
import {DYNAMIC_PERSONA_PROMPT} from '../src/dynamic-persona.js';
export function dynamicPersonaHTML(){return `<section data-view="dynamic-persona" hidden><div class="sy-top"><h3>动态人设</h3><button type="button" data-jump="api">独立 API</button></div><p class="sy-help">人设有自己的楼层周期，直接读原文，不等主总结。每次一条后台请求；主聊天只读取已保存档案。锁屏可能暂停，返回前台后接着处理。</p><div class="sy-card"><p data-persona-status role="status"></p><div class="sy-actions"><button type="button" data-persona-enable>启用 / 继续</button><button type="button" data-persona-pause>暂停人设</button><button type="button" data-persona-disable>关闭动态人设</button></div><div class="sy-grid">${setting('dynamicPersonaEvery')}${setting('dynamicPersonaKeepRecent')}</div>${field('当前聊天起算楼层','<input data-persona-start type="number" min="0" value="1">')}<p class="sy-help" data-persona-progress></p><div class="sy-actions"><button type="button" data-persona-save>保存人设设置</button><button type="button" data-persona-check>检查进度</button><button type="button" data-persona-retry>处理 / 重试下一批</button></div><details><summary>预设与请求预算 · 可 DIY</summary>${setting('dynamicPersonaInputUnits')}${setting('dynamicPersonaOutputTokens')}${setting('dynamicPersonaDeadlineMs')}${field('发给人设模型的指导词',`<textarea rows="12" data-persona-prompt>${esc(DYNAMIC_PERSONA_PROMPT)}</textarea>`)}<button type="button" data-persona-default>恢复内置预设</button></details></div><p class="sy-help" data-persona-worldbook></p><p class="sy-help">同一人物按阶段保存；修改整份档案，属性可自由增删。锁定后 AI 不覆盖。原世界书不永久停用；可识别的静态人设或安全 EJS 当前阶段在本轮请求替换，复杂脚本保留原样。</p><details class="sy-card"><summary>本轮动态人设注入</summary><p data-persona-injected-info></p><div class="sy-packet" data-persona-injected-text></div></details><div class="sy-actions"><button type="button" data-persona-profile-prev>上一组人物</button><button type="button" data-persona-profile-next>下一组人物</button><span data-persona-profile-page></span></div><div data-persona-profiles></div><details class="sy-card"><summary>新增 DIY 人物档案</summary>${field('姓名','<input data-persona-new-name>')}${field('完整人物信息','<textarea rows="6" data-persona-new-text></textarea>')}<button type="button" data-persona-add>新增并锁定</button></details><details class="sy-card"><summary>独立更新记录</summary><div data-persona-batches></div><div class="sy-actions"><button type="button" data-persona-prev>上一页</button><button type="button" data-persona-next>下一页</button></div></details></section>`;}
export function mountDynamicPersona({panel,app,run}){
  const $=s=>panel.querySelector(s),root=$('[data-view="dynamic-persona"]'),drafts=new Map();let stamp='',scope='',page=0,profilePage=0,startDirty=false,promptDirty=false;
  const bind=(s,fn)=>$(s)?.addEventListener('click',e=>run(fn,{name:'dynamic-persona',button:e.currentTarget}));
  $('[data-persona-start]').addEventListener('input',()=>{startDirty=true;});$('[data-persona-prompt]').addEventListener('input',()=>{promptDirty=true;});
  const save=async()=>{const patch={dynamicPersonaPrompt:$('[data-persona-prompt]').value};for(const input of root.querySelectorAll('[data-setting]'))patch[input.dataset.setting]=Number(input.value);await app.setDynamicPersonaStart(Number($('[data-persona-start]').value));await app.saveSettings(patch);startDirty=promptDirty=false;};
  bind('[data-persona-save]',save);bind('[data-persona-enable]',async()=>{await save();await app.setDynamicPersona(true);});bind('[data-persona-pause]',()=>app.pauseDynamicPersona());bind('[data-persona-disable]',()=>app.setDynamicPersona(false));bind('[data-persona-check]',()=>app.inspectDynamicPersona());bind('[data-persona-retry]',()=>app.processDynamicPersona());
  bind('[data-persona-default]',()=>{$('[data-persona-prompt]').value=DYNAMIC_PERSONA_PROMPT;promptDirty=true;});
  bind('[data-persona-add]',async()=>{await app.addDynamicPersona($('[data-persona-new-name]').value,$('[data-persona-new-text]').value);$('[data-persona-new-text]').value='';});
  bind('[data-persona-prev]',()=>{page=Math.max(0,page-1);stamp='';paint(app.state);});bind('[data-persona-next]',()=>{page++;stamp='';paint(app.state);});
  bind('[data-persona-profile-prev]',()=>{profilePage=Math.max(0,profilePage-1);stamp='';paint(app.state);});bind('[data-persona-profile-next]',()=>{profilePage++;stamp='';paint(app.state);});
  function paint(s){
    const d=s.dynamicPersona??{profiles:[],batches:[]},nextScope=JSON.stringify(s.core?.scope);
    if(scope!==nextScope){scope=nextScope;stamp='';startDirty=promptDirty=false;page=profilePage=0;drafts.clear();$('[data-persona-profiles]').replaceChildren();}
    $('[data-persona-status]').textContent=`${s.settings.dynamicPersonaEnabled?'已启用':'未启用'} · ${d.message??'打开聊天后读取档案'}`;
    if(!startDirty)$('[data-persona-start]').value=d.startFloor??1;if(!promptDirty)$('[data-persona-prompt]').value=s.settings.dynamicPersonaPrompt||DYNAMIC_PERSONA_PROMPT;
    $('[data-persona-progress]').textContent=d.plan?`${d.plan.coveredThrough>=d.plan.startFloor?`连续已处理 #${d.plan.startFloor}–${d.plan.coveredThrough}`:'起点后尚未处理'} · 下一批 #${d.plan.nextStart}–${d.plan.nextEnd} · 最新 #${d.lastIndex??'未读取'} · 保留 ${d.plan.keepRecent} 楼`:'独立进度尚未读取';
    $('[data-persona-worldbook]').textContent=d.mirror?.status==='saved'?`世界书镜像：${d.mirror.name}（存档查看用，不重复绑定注入）`:'世界书镜像尚未生成；需要宿主提供酒馆助手世界书接口。';
    $('[data-persona-injected-info]').textContent=d.lastInjection?`替换 ${d.lastInjection.replaced} 份阶段人设，补充 ${d.lastInjection.supplemental} 份；只是加入待发请求，不代表模型一定采纳。`:'还没有本轮注入记录。';
    $('[data-persona-injected-text]').textContent=d.lastInjection?.text??'';
    const visible=(d.profiles??[]).filter(p=>!p.deleted),pages=Math.max(1,Math.ceil(visible.length/6));profilePage=Math.min(profilePage,pages-1);$('[data-persona-profile-page]').textContent=`${profilePage+1}/${pages} · ${visible.length} 份档案`;
    const nextStamp=JSON.stringify([d.profiles,d.batches,page,profilePage]);if(stamp===nextStamp)return;stamp=nextStamp;
    const list=$('[data-persona-profiles]');for(const e of list.querySelectorAll('[data-persona-id]')){if(e.querySelector('details[open]'))drafts.set(e.dataset.personaId,e);else drafts.delete(e.dataset.personaId);}for(const id of drafts.keys())if(!visible.some(p=>p.id===id))drafts.delete(id);const editing=drafts;
    const cards=[];
    for(const p of visible.slice(profilePage*6,profilePage*6+6)){if(p.deleted)continue;if(editing.has(p.id)){cards.push(editing.get(p.id));continue;}
      const card=panel.ownerDocument.createElement('article');card.className='sy-card';card.dataset.personaId=p.id;
      card.innerHTML=`<h4>${esc(p.name)}</h4><p class="sy-help">${p.locked?'已锁定 · ':''}依据至 #${p.through} · ${p.bindings?.length?'阶段人设':'补充档案（不替换脚本）'}</p><div class="sy-packet">${esc(p.text)}</div><details><summary>修改整份人物档案</summary><textarea rows="12" data-persona-edit>${esc(p.text)}</textarea><label><input type="checkbox" data-persona-lock ${p.locked?'checked':''}>锁定，不让 AI 覆盖</label><button type="button" data-persona-commit>保存人物</button></details><div class="sy-actions"><button type="button" data-persona-undo>恢复上一版并锁定</button><button type="button" data-persona-delete>删除档案</button></div>`;
      const action=(sel,fn)=>card.querySelector(sel).addEventListener('click',()=>run(fn,{name:'dynamic-persona'}));
      action('[data-persona-commit]',async()=>{await app.editDynamicPersona(p.id,{text:card.querySelector('[data-persona-edit]').value,locked:card.querySelector('[data-persona-lock]').checked});card.querySelector('details').open=false;stamp='';paint(app.state);});
      action('[data-persona-delete]',()=>app.editDynamicPersona(p.id,{deleted:true,locked:true}));action('[data-persona-undo]',()=>app.undoDynamicPersona(p.id));cards.push(card);
    }
    list.replaceChildren(...cards);
    const batches=[...(d.batches??[])].sort((a,b)=>b.startIndex-a.startIndex);page=Math.min(page,Math.max(0,Math.ceil(batches.length/10)-1));
    $('[data-persona-batches]').innerHTML=batches.slice(page*10,page*10+10).map(b=>`<p>#${b.startIndex}–${b.endIndex} · ${b.status==='saved'?`已保存 · ${b.profileCount} 份更新`:`未完成：${esc(b.message??'可重试')}`}</p>`).join('')||'<p class="sy-help">还没有人设更新记录。</p>';
  }
  return {paint};
}
