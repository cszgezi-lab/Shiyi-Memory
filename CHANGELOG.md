# 更新记录

## 0.21.33

- **修 Android 导出经常失败的问题**：之前的导出在 Android 上会走宿主的原生 Downloads 桥（`saveFileToDownloads` / `requestCreateDocumentPicker`），这条路径在很多设备上抛 Java 异常或 `file_missing`，导致「配置 / 全局备份 / 聊天备份 / 注入日志 / 总结预设 / 正文提取 / 扩展区块」这一系列导出按钮点了之后看不到文件，或者弹出失败提示。现在统一改为「浏览器下载 / 分享」路径（`anchor.download` 触发系统的下载/分享面板，用户自己选保存位置），不再依赖那条不稳的原生桥；`download()` 默认就是这条路径，需要原生桥的调用方可以显式传 `{preferBrowser:false}`。提示文案也同步改成「已发出下载请求；在弹出的下载面板里选择保存位置，或直接点"查看／复制文本"自行保存」。

## 0.21.32

- **修 paint 阶段和 mount 阶段的 null-deref 崩溃**：经过 JSDOM 自动化挂载测试，发现并修复了以下启动/渲染崩溃：
  - `product-extraction-view.js`：mount 时 `panel.querySelector('[data-view="extraction"]')` 返回 null 导致后续全崩 → 加 `?.` 并 early return
  - `product-character-journal.js`：mount 时对缺失的 `[data-keepsake="dialogue"]` 或 `[data-keepsake="diary"]` 元素调用 `querySelector` → 加 `?.` 并 continue
  - `product-people-view.js`：mount 时根元素缺失 → 加 `?.` 并 early return
  - `product-summary-presets.js`：mount 时根元素缺失 → 加 `?.` 并 early return
  - `product-quality-view.js`：mount 时 `$('[data-quality-optional]').before(...)` 元素不存在 → 改为 `?.before()`
  - `product-knowledge-view.js`：mount 时 `$('[data-files]')` 返回 null 导致 `.files` 访问崩溃 → 加 null 检查
  - `product-dynamic-persona.js`：mount 时 `root.querySelectorAll(...)` 中 root 为 null → 加 early return
  - `product-runtime-log.js`、`product-batch-list.js`、`product-merge-view.js`、`product-injection-log.js`、`product-knowledge-view.js`：paint 函数中对缺失元素的 `.value`、`.textContent`、`.innerHTML`、`.disabled` 赋值 → 加 null guards

## 0.21.31

- **修 0.21.30 启动时报 `Cannot read properties of null (reading 'addEventListener')` 的问题**：部分视图（召回、批次、合并、日志、正文提取、字典、人设、世界、注入、人物、心迹、助手预设、设置等）的 mount 阶段会去找面板里的元素并直接挂监听；只要那一步查询返回 null，扩展整体就会立刻挂掉，留下一个红框错误条。这次给所有挂监听的位置都补了可选链，元素不存在时安静跳过，插件不再因为某一处缺元素就整页崩。

## 0.21.30

- **修复 0.21.29 一进创建阶段就崩溃的问题**：保存批次后曾误调一个不存在的 `wakeVectors()` 函数，扩展加载时立即抛 ReferenceError 整个挂掉。已移除那一行，保存-写入-刷新这条主路径不再受影响。
- 导出运行日志改走「浏览器下载/分享」路径，不再误以为已落到 Downloads；提示文案同步改成「已交给浏览器下载/分享」。
- 总结页导航改为 6 项：记忆 / 人物 / 总结 / 召回 / 日志 / 设置（助手下线、日志独立成模块）。
- 总结页两个扇形图卡只保留标题、缺口、已总结范围、下一批按钮；详细数字和图例默认隐藏。
- 总结页「事件合并」tab 已移除。

## 0.21.29

- **修一个会让发送卡很久的问题**：查询向量与重排原本只受 provider 的「请求截止时间」（默认 120 秒）约束，接口一旦不回答，每一轮发送都会被拖住。现在这两条在线通道各守自己的上限（默认各 4 秒，可在设置里调），整轮召回还有一道合计上限（默认约 9.2 秒，可用「在线总超时保护」收紧）。
- 超时不再等于失败：在线通道超时或整轮超时都会**自动改用本地检索结果继续注入**（记忆不会整轮消失），并在运行日志与本轮注入里标明「在线检索未完全可用」，不阻塞聊天。
- 从日志能看到差别：以前是 `recall failed · stage=request · TIMEOUT`（拖到两分钟），现在最坏是「查询向量超时」的一条 warning，注入照常完成。
- 不改模型、提示词、注入预算、自动周期、API/DIY 预设、原世界书和已有记忆。

## 0.21.28

- 运行日志的「记忆召回」条目补齐可对照的耗时：每次注入都记录本轮准备总耗时与本地检索、向量、重排各占多少，配合该条记录自带的时间戳，可以和「等待共享请求队列」「发送模型请求」的时间直接对齐，判断慢在注入、排队还是模型本身。
- 只记耗时与计数，不含查询原文、聊天正文或密钥。
- 不改模型、提示词、注入预算、自动周期、API/DIY 预设、原世界书和已有记忆。

## 0.21.27

- 运行日志新增**每次注入的耗时明细**：写清本轮准备注入用了多久，本地检索、向量、重排各占多少，以及在线等待上限是共享还是各接口独立。回复变慢时可以直接看到卡在哪一步。
- 该条记录只含耗时与计数，不含查询原文、聊天正文或密钥。
- 不改模型、提示词、注入预算、自动周期、API/DIY 预设、原世界书和已有记忆。

## 0.21.26

- **总结页按确认的草图重做**：顶部只有五个按钮（手动总结 / 自动总结 / 总结预设 / 批次管理 / 事件合并）；「记忆总结」与「动态人设总结」各一张卡片，用**扇形图**显示已总结、缺口、按设置不记录、未到四块比例。
- 卡片上的按钮**按已保存的设置算出真实范围**：例如「总结下一批 #61–70」「总结下一批 #41–50」，有缺口时才出现「补缺口」；记忆走主总结，人设走它自己的手动补建。
- **补完缺口后起算楼层自动推进**：只跨过真正连成一片的已记录楼层，中间有洞就停在洞前；撤下批次时退回，旧行为不变。
- **运行日志移到总结页最下面**（不再占页签），一页 120 条；「查看运行日志」直接带用户到那一段。
- 运行日志新增**召回耗时**：每次注入都会记录本轮准备用了多久、其中本地检索/向量/重排各占多少、以及在线等待上限，方便判断回复慢在哪一步。
- 「检查进度」「补采缺口」仍在总结页顶部；「新增属性」会正确展开新增记忆表单。
- 不改模型、提示词、注入预算、自动周期、API/DIY 预设、原世界书和已有记忆。

## 0.21.25

- 修掉页面改版留下的两处失效引用：记忆页「新增属性」原来会去找已经不存在的折叠块（点了没反应），现在会正确展开工具栏里的新增记忆表单；人物页旧版档案统计/分区的样式规则清掉。
- 召回字典补齐：地点名从事件自己的「地点」字段兜底收录（原来只有模型写进 entities 的名称才进字典，所以 120 楼里常常只有角色名）。只在名称确实出现在该条正文里、且不是一整句话时才收录，不改任何记录。
- 字典页顶部显示当前收了哪些类别（人物／地点／组织／物品／术语各几条），只有人物时会明确提示“地点、组织、物品要看总结是否按合同写进 entities，地点已兜底”。
- 不改模型、提示词、注入预算、自动周期、API/DIY 预设、原世界书和已有记忆。

## 0.21.24

- 按钮只留中文：记忆页「新增记忆」、人物页「新角色」、正文提取页「新增规则」都不再带「＋」号。
- 修一个真机导出归因错误：直存失败后改用系统文件选择器、选择器又失败时，之前报的是**直存**的失败阶段，会让人误以为文件已经写出去、只是发布失败。现在按“最后一次真正失败的那一步”报，并把「宿主的暂存文件写入失败」单独报出来（原来会笼统说“都没能保存”）。
- 不改模型、提示词、注入预算、自动周期、API/DIY 预设、原世界书和已有记忆。

## 0.21.23

- 记忆页顶部新增**进度圆环**：显示已记录到第几楼、还差哪些楼层、下一批范围与上次成功时间；圆环与文字都来自已保存的记忆和自动总结记录，不猜、不预测。
- 工具栏收成一行：＋ 新增记忆 / 搜索框 / 全部 N 条。新增表单仍是原来那一份，点开才展开。
- 磁贴改成**数字在上、名字在下**（完整名字，不再缩写），并把「资料」也放进来，共 6 个；分类筛选下拉仍在。
- 新增**最近修改**一栏：列出最近 3 条有来源的记录与楼层，点一下就打开那条的编辑。
- 不改模型、提示词、注入预算、自动周期、API/DIY 预设、原世界书和已有记忆；记录不迁移、不重写。

## 0.21.22

- 人物模块重做：顶部一条可横滑的角色条（左边就是搜索框，右边「＋ 新角色」），下面是这个角色的全部内容。
- 一个角色里的每一段都是同一种「一行一条」的字段：人物属性、动态人设、关系、约定、人设变化、角色心迹、关键台词。点这一行文字展开全文，展开后再点一次进修改；长内容默认只显示一行或三行。
- 人物属性回到 0.21.15 的档案样式：身份、学籍、外在形象、居住地、身体能力、随身物品……一行一项，点开看全文与来源；不再按重要度加星（属性是事实，不是事件）。总结仍会自动新增任意客观属性。
- 角色名旁边是 改名 / 合并档案 / 删除；删除会把这些记录移入回收站，聊天原文与总结不受影响。
- 全项目配色统一换成「靛青纸白」：靛青重点色、纸白底、统一圆角与字体；按钮从 44px 方角改成更小更圆的尺寸，字段旁边不再出现比正文还大的按钮。


## 0.21.21

- 记忆列表改为**重要度优先**：先 ★★★★★，再 ★★★★☆ ……，同一等级里再按模块轻重与楼层新旧。排序只读元数据，不会为了排序去展开没打开的记忆正文。
- 每条右上角直接一个「修改」，一次点到就地编辑；「操作」里只保留删除与不再召回，不再出现两处修改。
- 卡片压紧（标题 14px、简介最多两行、间距收紧），390px 一屏从 2–3 条变成 3–4 条。
- 每页默认 10 条，可选 10 / 20 / 50；搜索仍覆盖标题、标签与召回速览，需要时才读正文。
- 新增记忆与修改记忆的表单加上透明示例字（照原文写清谁、何时、做了什么、结果如何），填了就消失。
- 人物页每条心迹/台词可就地编辑（走原有角色记录写入，不会覆盖整条正文）；每人一个「改名」，旧名保留为别称，已有记录仍归同一角色，可在设置 → 自动字典改回。
- 不改模型、提示词、注入预算、自动周期、API/DIY 预设、原世界书和已有记忆。

## 0.21.20

- 按职责重新分页：记忆页只留时间线——事件、知情、楼层摘要、演绎参考、疑点、资料；人物相关的四类（人物与事实、关系、约定、人设变化）归人物页。
- 人物页在每个角色下新增「关系」「约定」「人设变化」三段，和原有的人物属性放在一起；每条显示重要度星标、来源楼层与正文，点「修改这条记录」直接改，保存后用于召回，原始来源保留。
- 只是显示归属改变：记录仍然存在同一份存储的同一类别里，不迁移、不改写、不重跑总结；记忆页的分类下拉仍可选中这四类并正常编辑，所以从检索或旧习惯进来不会找不到。
- 记忆页磁贴从九个减到五个（时间线），48px 高、13px 字与配色沿用已发布版本，未改版式。
- 不动模型、提示词、注入预算、自动周期、API/DIY 预设、原世界书和已有记忆。

## 0.21.19

- 设置页顶部新增「更新中心」：一行显示总结进度（已记录至第几楼、下一批、待处理批次）与人设进度（依据至第几楼、下一批、是否暂停），旁边四个按钮直达「补采未记录楼层 / 总结下一批 / 动态人设设置 / API 与模型」。
- 更新中心只汇总进度与跳转，不放第二份开关：勾选框、周期、执行按钮仍只有一份，避免同一动作出现两处、也避免误触重复运行。
- 隔离手机验收新增该面板的真实点击步骤（进度文案、跳转人设面板、跳转总结面板）。
- 不改模型、提示词、自动周期、API/DIY 预设、原世界书和已有记忆；无需清库或重做总结。

## 0.21.18

- 人物页的档案、心迹、台词条目也显示重要度星标（与记忆页同一套颜色），一个角色的重要信息不用点开就能扫出来。
- 星标只用于显示：不改变检索、注入、总结或人设的任何判断。
- 不改模型、提示词含义、自动周期、API/DIY 预设、原世界书和已有记忆；无需清库或重做总结。

## 0.21.17

- 记忆与人物条目加重要度分级：五级星标，颜色随等级变化（★★★★★ 暖橙 → ★☆☆☆☆ 暗灰），重要事件一眼跳出来。总结模型可给 1–10 的 importance；没给（旧记忆）或给得不合法时由程序按参与者、来源楼层、关联事件推导，不需要重跑总结。
- 记忆模块九个磁贴恢复完整（含之前被漏掉的“楼层摘要”），并改用短名避免窄屏折行；“人物与事实”等完整名字在条目与其它界面照旧显示。
- 记忆页搜索框与分类下拉改为各占一行：390px 下搜索框以前被下拉挤到只剩几个字。
- 不改模型、总结/人设提示词含义、自动周期、API/DIY 预设、原世界书和已有记忆；无需清库或重做总结。

## 0.21.16

- 世界书不再需要手动点“检查世界书读取”：打开聊天后自动读取角色卡与聊天绑定的世界书，并自动给出每个条目的归属结果（已识别人物归属 / 公共规则 / 归属未确认）。
- 人物档案里的“原书来源与本次修改”现在会说明**为什么**没有可替换的原书内容：宿主没有读取接口、角色卡没绑世界书、条目里没有人物名、或条目是脚本模板；并列出已读到的条目名，不再只显示“0 个条目”。
- “本次改写原设定 N 处”现在会说明：若 N 为 0，代表模型只追加了剧情变化、没有去改写和剧情冲突的原设定句子。
- 日志导出加入“只看失败（最近 50 条）”：只给最近 50 条失败/提醒的精简记录（每条只保留定位字段，去掉堆栈与原因链），实测 10 万字的日志导出后约 1.5 万字，手机上可直接读取和复制。
- 不改模型、自动周期、API/DIY 预设、原世界书和已有记忆；无需清库或重做总结。

## 0.21.15

- 动态人设日志现在会记录“本次改写了几条原设定句子”和“保留了几条原设定句子”。原世界书本身永不被修改，改的只是这次请求里生效的动态档案；这两个数字能让你在日志里直接确认模型到底有没有动原设定。
- 如果你的档案长期只累积“当前剧情变化”、改写数为 0，说明模型没有去改写原设定里冲突的句子——这是下一步要强化的方向。
- 其余不变：只看失败并复制的日志快照、人设完整累积档案、合并进度续接；不改模型、自动周期、API/DIY 预设、原世界书和已有记忆。

## 0.21.14

- 新增“只看失败并复制”：运行日志页可以只导出失败与提醒记录，手机上直接复制。以前无论怎么删日志，导出都是最多 2 MB 的全量快照（实测 10 万字符），根本没法看、没法复制；现在同一份日志只留失败记录后是 1 千字符左右，并且保留全部定位字段（失败阶段、走的哪条通路、哪个宿主桥方法抛错、导出体积）。
- 过滤快照不再附带 retention/limitations 等大段元数据，只保留能定位问题的记录本身。
- 上一版的人设累积修复继续有效：模型每批看到的是完整累积档案。
- 不改模型、自动周期、API/DIY 预设、原世界书和已有记忆；无需清库或重做总结。

## 0.21.13

- 修复动态人设长聊天“只记得最新一段”的真实缺陷：此前每批只把上一批的变化片段当作“已有档案”发给模型，原世界书设定和更早批次都不在模型可见范围内；现在发送的是完整累积档案（原设定保留部分＋此前各批变化），并明确要求返回“基于它更新后的完整档案”。玩到 60 楼不会再只剩 51–60。
- 人设合同补充：previous 是完整累积档案而非单批片段，返回时必须保留仍然有效的旧内容，不得只写本批新增，也不重复抄写原设定原文。
- 新增回归：连续 6 批（1–10 … 51–60）后，每批请求都仍包含原书设定与第 1–10 楼内容，档案只增不减。
- 导出失败归因补充：日志现在能区分“失败发生在调用宿主桥之前（暂存文件阶段）”与“桥方法自身抛出”，便于定位真机导出。
- 不改模型、提示词外的其它设置、自动周期、API/DIY 预设、原世界书和已有记忆；无需清库或重做总结。

## 0.21.12

- 导出失败诊断可定位到具体宿主步骤：运行日志现在记录“失败发生在哪一步（校验暂存文件／写入文件／发布到系统下载目录）”“走的是直存还是文件选择器”“实际调用到哪个 TT 桥方法、抛的是哪类异常”，以及导出体积。此前这几项会被错误地丢掉，所以只能看到一句外层 Java 异常。
- 导出加超时保护：宿主导出若在限定时间内没有任何返回，会明确报超时，不再无限等待。
- 已核实：0.21.2 能成功导出，而插件侧导出代码自 0.3.0 起未改动，问题在 TT 直存这条通路；本版先让失败自我说明落在哪一步，便于一次定位。
- 其余不变：直存失败仍自动改用系统文件选择器保存，合并后动态人设进度续接；模型/提示词/自动周期/API/DIY 预设/原世界书/已有记忆都不改，无需清库或重做总结。

## 0.21.11

- 真机反馈确认：TT 直存 Downloads 的原生接口在部分设备上始终抛 Java 异常。本版在直存失败时自动改用系统文件选择器保存（TT 自带的第二条通路），会弹出“保存到…”让你选位置，成功显示实际保存位置；两条通路都失败才报失败，仍保留“查看／复制文本”。
- 修正导出结果字段归一化：官方运行时返回 savedPath、裸桥返回 saved_path，之前会把成功的保存误判为“未确认”，现已统一。
- 0.21.10 同号重发无法在页头确认手机是否真的换包；本版提升版本号以便直接确认。
- 其余保持不变：合并后动态人设进度续接、重建不复活被合并档案；模型/提示词/自动周期/API/DIY 预设/原世界书/已有记忆都不改，无需清库或重做总结。

## 0.21.10

- 修复 TT Android 导出：宿主“直存 Downloads”的原生接口在部分设备上会抛 Java 异常。现在失败时自动改用系统文件选择器（TT 自带的第二条保存通路）保存，成功会显示实际保存位置；两条通路都失败才报失败，不再出现失败与成功并存。
- 修复导出成功状态误报：只有宿主明确确认文件已保存才显示成功；取消、系统拒绝保存权限、宿主异常、暂存文件缺失和普通原生失败各自只有一种结论，不再把“已派发”当成“已保存”。
- 导出失败时保留“查看／复制文本”出口，并在运行日志记录脱敏后的保存阶段（校验暂存文件、写入文件、发布到系统下载目录），便于下次直接定位是哪一步失败。
- 修复合并人物后动态人设停在合并前进度：合并档案的正文由玩家保留，依据楼层会随手动补建和自动更新继续推进。
- 修复重建范围时复活被合并的旧档案；世界书镜像只写入仍活动的档案。
- 不改模型、总结/人设提示词、自动周期、API/DIY 预设、原世界书和已有记忆；无需清库或重做总结。

## 0.21.9

- 修复合并重复人物后动态人设停在旧楼层：合并只继承保留档案的锁定策略，不再把被吸收档案的锁定状态带到目标档案；新增合并后自动处理新楼层的回归覆盖。
- TT 原生文件导出失败时增加一次宿主下载桥回退；回退结果显示为“已交给 TT 下载桥重试”，权限拒绝、取消和回退失败仍保留可复制日志及精确失败状态。

## 0.21.8

- 待确认称呼（如“紫阳花”）在人物详情顶部直接显示归入入口并默认展开；若只有一个全名包含该称呼，会预选候选但仍需用户确认。
- 明确区分“归入称呼”和“合并正式档案”：待确认称呼不会伪装成第三个人，也不会出现在正式档案合并列表；确认后若已有同名正式档案就自动调用可撤销的整档合并，否则保存为聊天内手工别称。两种路径都会让心迹、台词、任意属性和来源统一显示在目标人物下，并抑制自动词典产生的重复人物；原始记录、原世界书不改，也不调用模型。

## 0.21.7

- 人物页始终显示明确的“动态人设档案”区块，不再让动态人设入口只出现在另一个页面或被“尚无动态档案”淹没。
- 歧义称呼可在人物页选择已有正式档案并保存为手工别称；重复动态档案可在同一区块直接合并。两种操作都不调用模型、不修改原始聊天或原世界书，且保留可恢复版本。

## 0.21.6

- 人物阅读页的动态档案区直接提供“合并重复人物档案”入口，解决入口只在动态人设管理页、难以发现的问题。
- 合并入口默认折叠并放在动态档案之后，保持窄屏首屏阅读空间；合并仍保留全名、别名、内容、来源和可恢复版本，不修改原始聊天或原世界书。

## 0.21.5

- 人设模型返回短名或别称时，只有选定楼层中能唯一对应正式姓名才自动校正；正文同时出现多个候选人物时不按出现次数猜测，歧义档案单独隔离，不再拖失败整批或阻塞后续批次。
- 人物页新增重复档案合并：可将“紫阳花”并入“濑名紫阳花”等正式姓名档案，合并内容、来源、别称、原书绑定和楼层依据；被并入档案可通过目标档案的“恢复上一版并锁定”撤销，原始聊天与原世界书不改。
- 人物导航允许从具体档案进入编辑，即使同一称呼仍有未确认记录；歧义的泛记录仍不会被错误归给某个角色。

## 0.21.4

- 日志导出改为当前内存快照，不等待任务和日志写盘完成；标明仍在写盘/记录中的状态。日志读取和任务末尾的日志等待有超时保护，不因日志存储无响应无限阻塞业务。
- 日志页提供“查看／复制文本”；宿主文件保存或权限失败后自动展开文本。原生保存失败不冒充成功，复制失败可手动选中文本复制，无需重新运行任务。
- 动态人设记录出错步骤、是否已请求模型及脱敏错误详情，在人物页也可直接查看。保存失败状态再出错时保留最初原因与已有档案；内部未就绪的继续/重试入口先重新加载校验，不绕过来源保护。
- 不改模型、总结/人设提示词、自动周期、API/DIY预设、原世界书和已有记忆。此补丁不保证外部服务、宿主永久读取故障已修复。

## 0.21.3

- 修复动态人设在原文读取失败后卡在首批：旧版 `HISTORY_UNAVAILABLE` 失败记录可自动恢复，无需删除档案、重建计划或改变自动周期。
- 聊天末页/分页读取失败先在同一聊天内有限重读，从新末页重新获取分页游标；不跳过缺楼或放松来源检查。持续故障停止定时重试，等下一次回复或回到前台再恢复，暂停/切聊仍会阻止任务。
- 自动总结共用读取恢复；检查楼数不再要求最新一楼已结束生成，保留楼层仍不送总结。等待、跳过和保存成功分开记录，并记录脱敏读取位置与尝试次数。
- 升级保留已有记忆、API、自动起点/周期、DIY预设、世界书与界面字号。不修改人设模型、总结提示词或召回策略。

## 0.21.2

- 修复酒馆隐藏原生文件框导致资料无法选取：在“导入资料”标题下提供可见的“选择文件”按钮，同步打开本机文件选择器。
- 显示已选文件名，支持多选和清除；未选文件时禁用预览，更换/清除后旧预览失效，防止误导入。选择和预览不调用模型，确认导入流程不变。
- 不改变九模块字号、既有资料、API或DIY预设。本次按维护者要求直接发布，未执行测试。

## 0.21.1

- 真实联动验收发现原文补充召回会带回已过滤的未选分支。现在本地检索、重排原文和最终注入统一遵守正文提取配置；旧记录也即时应用，不重写原始聊天/来源hash，不需重新总结。切换规则会失效对应召回缓存。

- 真实模型验收发现并修复一种多余重试：知情记录将本批事件ID误放进属性链接字段。仅在该事件唯一、不与属性重号且没有竞争事件链接时，本地纠正为事件链接；保留命题及原始来源，不新增模型调用。
- 未知ID、歧义、冲突链接和来源错误继续校验；记录本地纠正计数，原始模型回答不改。升级不重写已保存记忆、API、DIY预设或原世界书，不改变0.21.0字号与布局。

## 0.21.0

- 记忆默认只显示分类与搜索，点击类别才展开，再点收起；支持每页 10/20/50 项（默认 10），完整人物档案作为一项分页，不再截断前 100 项。移除重复的人物工作区入口；九个模块保留原有小字号与高度，仅将内容校对、新增记忆、扩展模块标题缩为相同的 13px，保留触控区域。
- “记录”改名“总结”。增加已记录 / 未记录楼层覆盖视图与一键补采；按已保存的自动总结起点、批大小及保留楼数只处理缺口，末批可不足一批。不启动自动开关、不重做已成功楼层，全部待处理范围先入队，失败后可继续。
- 人物自动与手动更新集中在“更新人物”；API、预设、请求预算、原书识别单独收进“人设设置”。已有台词 / 心迹参考与完整动态档案注入保持，不增加发送时的人设模型请求。
- 世界与知识库统一为导入资料，移除原创 / 同人选择和重复手动字典。默认分段、分析字典与索引已预填，预览及确认导入按钮常驻；新增独立资料分析 API，默认沿用配置助手连接，原向量服务独立。
- 已导入资料支持分页查看和就地编辑；只写变更片段，原内容保留，关键词即时刷新，界面保存后更新字典和变更的向量；旧索引不伪装新内容，失败保留原文、可继续补建。
- 变量与剧情日期独立设置，解释自动读正文 / 默认不额外扫变量，可选路径模板需用户确认。升级保留 API、预设、空值与 DIY，不猜填故事日期或卡的变量路径。

## 0.20.2

- 正文新角色与原书人物同等参与动态建档；显式说话人候选辅助发现，不另开识别请求。
- 档案内保留有来源、分对象的成长起点、前后状态和转变缘由；同主题增量更新，其他主题保留，旧版本仍可恢复。
- 无原书人物的首份档案与后续新增属性也转为可局部修改的聊天底稿，避免模型只写本批变化时丢失已有信息；明确自称的昵称可沿用。
- 新角色同样具备简繁/别称、原话与服装情境、私密边界；成长证据缺失不阻断整批或自动创建校对任务。
- 原世界书、旧档案与自定义预设不在升级时重写；整档 DIY/锁定保留，发送时不增加人设模型调用。

## 0.20.1

- 真实模型测试修正：人设语料兼容模型多带的一层完整引号或中文译文括号；仍须逐字对应原楼层和说话人，私密心声、虚构台词、错人及拼接双语仍拒收。
- 兼容 GAL 角色名与紧接下一行引语的排版，不跨越旁白、空行或被过滤内容建立说话人归属。
- 双语读取提示不再使用可被模型抄成台词的“日文/中文”占位示例；明确只提取实际台词正文，不增加模型调用，不改变已有预设或原世界书。

## 0.20.0

- 新增“设置 → 正文提取”：可编辑标签/安全正则、保留/排除、捕获组和启停，提供 JSON 导入导出、免费逐楼或粘贴预览。提取只生成模型阅读副本，原始聊天、证据位置与哈希不改；未配置保持原行为。规则随已开始总结冻结，升级不覆盖旧预设/API/记忆。
- 可选完整双语配对优先中文，沿用 content、time_format 和 GAL 中文译文；可导出轻量语境/私密标记的仅显示正则。时间、叙事语境和私密边界受保护；危险规则、缺匹配、超安全工作预算或空结果提示并回退安全正文，不悄悄截断。预览不是语义等价保证，也不保证模型加速幅度。
- 主导航整理为故事、人物、记录、召回、助手、设置；人物工作区汇集同一人的完整档案、心迹、台词及自由属性，保留全部编辑、来源、历史与开关。更新/API/手动补建收进折叠设置，减少默认页面操作堆叠；私密心迹不混为公开对白，不增加模型请求。
- 修复动态人设对 GAL 日文后中文译文的误拒；只接受能在局部原文确认说话人的完整原话，支持简繁/唯一别称，仍排除他人对白、旁白、内心及规划块。补充倒叙时点和当前关系对象的提示，不自动猜改年龄、学段或好感。
- 原世界书/EJS/MVU、聊天隔离、锁定档案和已保存总结保持原样，不需要清库重做。当前版的本地与模拟宿主验收不等于任意模型、手机和角色卡的语义零错误。

## 0.19.0

- 动态人设改为“原文底稿＋有来源的局部修改”：程序保留未修改的原文，不再依赖模型每批重写整篇传记；同角色多个专属条目归入完整档案，旧局部修改跨批沿用。人物字段仍可自由扩展，整档DIY、锁定、恢复和聊天隔离保留。
- 原书归属结合标题、明确姓名字段、已确认姓名关键词与简繁/简称；公共规则、多人物混合与无法安全解析的脚本不接管。原世界书及启用状态不变，只替换当前请求中已识别的人设文字；镜像仅用于查看。
- 保留原有外貌、口吻和未变设定；提示模型区分稳定体貌、当时衣着与关系对象。完整正文外附有界的逐字变化线索，提醒换装、关系和属性变化，不替模型断言事实，不增加模型请求。
- 对可明确归属的简短换装/外观变化，保留带楼号的逐字场景参照，防止模型略过一句换装便只剩旧衣服。参照不是永久人设或当前穿着指令，计划、代词/多人归属不明时不猜补。
- 接入已有关键对话与角色心迹参考；语料需对应真实说话来源，已核实原话可按阶段沿用或撤下。私密心声、推测和用户编写保留性质，不当公开台词或额外知情。
- 人物下可查看原书来源、局部修改前后及依据楼层；世界书读取可展开归属/未接管原因，与本轮实际注入分开显示。升级不重写用户预设、API、旧档案或记忆，无需删除主总结。旧/手工档案保持原样，下次显式或已启用的周期更新才建立保留底稿。

## 0.18.5

- 动态人设由程序按已确认人物身份绑定本次提供的全部专属安全原设定，不再把模型抄写的编号作为替换权限。兼容旧字段但不能跨人/跨书越权，保留原文楼号、原书版本、脚本和聊天隔离校验。
- 手动补建失败/释放忙碌不再等待运行日志落盘；后续批次保留、可以继续。排队与保存完成分别提示，启动后不误报范围改变，空镜像明确尚无人物档案。
- 人设校验诊断记录具体档案序号、失败字段与程序绑定数量，中文展示，不导出人物正文/密钥。补多角色大世界书20楼真实合成、错号/跨人引用、失败恢复与手机界面回归。

## 0.18.4

- 动态人设默认改为“剧情主导 · MVU 作参考”：人物随已发生的互动成长，不因数值没跨阈值否定正文中已经确认的关系；数值也不能凭空产生共同关系或知情。不修改 MVU 变量、阈值与 EJS 代码，保留严格阶段兼容选项。
- 同角色多个专属原条目合成一份完整当前档案；跨数值阶段沿用角色与旧档案 ID，不每次另起人物。同角色完整人设在本轮只替换一次，历史版本保留。默认替换不等待 MVU/人设模型、不回扫聊天；复杂脚本不擅自改写。
- 简繁姓名、可唯一识别的简称、字典别名和原文明确称呼共同匹配；共享昵称不猜人。整档编辑可就地修改别称，任意属性/手工锁定/上一版恢复保留，姓名匹配不改原文或模板路径。
- 旧总结预设、动态人设DIY提示词、API、预算与聊天记录不重置；新行为由独立主次设置控制。增加身份、阶段切换、实际投放、700楼本地替换、移动端编辑及旧版升级重启回归。

## 0.18.3

- 动态人设增加独立“手动补建”：起止楼层、每批楼数、免费请求计划预览、预存完整队列、后台逐批保存与失败继续；不受自动周期/保留楼数影响，不改变主总结。
- 补建使用候选进度，全部成功后应用，避免旧聊天回填中途把当前人设退回早期；原档案/DIY保留，候选可归档放弃。重复范围整体替换旧人设批次，来源修改撤回对应候选并可继续修复。可显式接续自动起点，自动仍暂停。
- 手动批次十条一页，空继续不挂“运行中”，检查世界书读取及同步镜像都有独立入口，不调用模型；镜像失败状态保存，档案不丢失。
- 保持原世界书不永久修改/停用、只替换可识别的当前阶段、复杂 EJS 保留与 MVU 只读。增加完整应用手动队列/宿主世界书接口、当前阶段/关闭/切聊天/读回失败/同名用户条目保护及移动按钮回归。

## 0.18.2

- 修复保存过旧总结预设后，新增时间/台词规则使全局设置校验失败、聊天无法打开的问题。旧规则基线固定，新增非结构性规则自动补推荐值；用户已有文字、留空项、原存档与API不重置，未知结构性规则仍拒绝。
- 预设界面补齐时间参照、台词来源、知情证据、承诺边界与角色心迹五项DIY入口；导入旧预设后直接展示迁移结果。
- 动态人设进度显示完整待处理批数与每批请求成本。明确旧聊天不继承主总结进度，移动起点会跳过早期原文；没有自动把已有总结转成人设基线，也不改变用户起点或后台开关。

## 0.18.1

- 人设原设定改用简短条目编号，共同底色与安全当前阶段由程序绑定。旧回答误用阶段号时仅兼容唯一、同人物的已提供引用；外人、未知编号和脚本仍拒绝。
- 待发送人设标记绑定聊天及请求版本；切聊天、阶段切换和人工改档后不把新聊天/新版本人设加入旧请求，失效标记回退原文。缓存仅保留有界来源与档案指纹。
- 关键台词增加独立说话来源，避免跨楼关系遗漏实际引用楼导致误删。重复原话保留已核实的来源范围，编辑、停用与重载保留。
- 总结输入附正文场景时间参照；满足同一明确时刻、动作及唯一来源时，本地纠正日期错位并记录数量及存档依据。历史回忆、计划、含时区时间及不确定情况不猜改，不增加模型调用。不自动改写用户旧存档。
- 收紧人设对对象/目的/时间边界的泛化；常见范围、观念性质使用中文展示，旧字符串空地点不显示为“null”。自动/手动周期、预算、自由属性、全量人物注入保持原设置。

## 0.18.0

- 新增独立动态人设后台流程：开关、周期、API/Key、预设、预算、楼层进度与失败恢复和主总结分开；无变化也推进已处理范围，临时错误最多自动尝试3次，内容过滤不热循环。
- 原设定与本批有效正文共同更新当前阶段档案；静态人设/安全简单EJS分支按本轮请求替换，原世界书和MVU不永久修改。复杂脚本不能安全分离时保留原样。新世界书是按聊天隔离、禁用的查看镜像。
- 完整人物档案DIY编辑、锁定、删除、上一版恢复、分页、本轮注入查看；旧响应不能覆盖用户刚刚编辑的档案，切聊/原文变化/阶段变化继续校验。
- 修复启用自动总结未立即检查及未追上后续完整批的问题；聊天绑定生命周期与单次总结取消分离，暂停主总结不取消独立人设。关闭自动总结时MVU仍刷新。
- 扩展独立模型设置、凭据持久化、内置助手规则与脱敏日志；旧记忆无需删除或重新总结。新增功能默认关闭，需自行配置人设API后启用。

## 0.17.10

- 大向量库改为小块增量存档：先验证新块再发布目录，保留旧索引与已付费回答的恢复路径。小旧索引继续兼容；状态只读取目录，召回按块扫描并保留完整分段与浮点精度，不改变总结次数。
- 移除每个新向量反复验证整库的开销；批次关联改为一次反向索引，批次视图按数据版本复用，日志与进度不再重复克隆整页批次正文。
- 后台单任务、小步续建；临时503/429/超时/断网按冷却自动重试，前后台和联网恢复会唤醒。认证及本地存储错误不热循环；用户停止、切聊天及关闭自动维护仍受尊重。
- 明确索引计数是记忆记录数而非总结调用次数；手动批次维护收进折叠区，保留DIY、单项/整批重试及重建接口。

## 0.17.9

- 修复重选旧范围只插入新批次、未更新旧排队状态的问题。整段计划先持久保存，每批保留原范围及每批楼数；后续批次在失败、停止和重启后仍可继续。未完成为空时收尾提示，不再一直显示“总结中”。
- 批次分别显示总结保存与向量覆盖状态，新增只补建本批／全部未完成索引。按当前有效内容和合并后的来源关联索引；保留成功向量与分段缓存，不调用总结模型，不删除旧记忆。
- 日志默认展示中文模块及问题、剩余队列、可读耗时和重试方向；代码堆栈与字段路径折叠，完整脱敏导出保留。向量服务繁忙明确指向索引重试，不误导重做总结。
- 兼容逐楼摘要的单一显式来源列表，仍检查来源版本、哈希、分片和楼号，不按位置猜测。旧回答带错误分片或缺来源时，显式继续重新请求当前失败批，避免无限复用无效回答。

## 0.17.8

- 默认总结使用酒馆兼容流式请求，不强制 `response_format` 或 `stream_options`；不拆楼数、不降低回复上限、不增加正常主请求次数，JSON 内容与来源验证保留。推荐方式升级后生效，旧 API/Key/预设/记忆保持不变。
- 新增兼容非流式与旧版 JSON 模式，使用不支持流式的接口时可切换；旧流式开关仅控制旧版模式。总结、可选分工与原文复核均收到实际传输设置。
- 兼容只有 `getReader()` 的手机流，保留中文/表情的跨字节解码。收到首段数据只报告进度，完整结束前不提交；取消、途中错误和截断仍按失败保护已保存记忆。
- 日志增加响应头/首段数据时间、请求消息数量、JSON 模式及脱敏接口模型指纹；识别受限层级的嵌套上游错误，不输出原始错误正文、聊天或认证信息。不把 502 一律猜成上下文或内容过滤。

## 0.17.7

- 将原文段号证据接入普通单次主总结：人物语境与具体获知内容可分段引用，程序按原始偏移回填，不再要求姓名和内容挤在同一句摘录中。不增加默认模型调用，不改旧存档。
- 提取时同时检查实际知情、明确未知和后来获知；部分获知保留看见的结果及未见过程的限制，避免保护规则反过来令有效信息全部留空。未知日期或渠道不强行编造。
- 段号只能绑定本条已核实的来源。无效证据仅使对应知情留待确认；有效内容和逐楼摘要可保存。仅兼容真实存在于唯一完整来源中的程序段号，实际聊天分片、跨来源和无效段号继续严格校验。
- 明确连续批次的来源规则：历史记忆中的来源不等于本批可引用原文，旧约定的新进展依本批明确证据记录，避免误复制旧来源导致保存失败。
- 复用原文解析与缓存，后续总结上下文只带获知范围，不重复发送整套长证据。人工编辑、DIY、完整人物注入和旧记录独立校对继续保留；模型仍可能漏记细节，不承诺任意正文零校对。

## 0.17.6

- 新增可调整的“每次校对条数”，默认 6，仅后台校对按小组处理，预览显示固定调用数；原主总结批数和前台召回不受影响。减少同时判断大量命题造成的旧内容复述与跨来源混淆。
- 知情专项校对按“人物—命题—本人来源”就地组织，先检查听见、看见、阅读及离场/遮眼/未读等限制，再确认或缩窄原命题。证据用每条专属短编号，不再要求摘录句重复角色全名；获知者、来源、原文切片与人工编辑保护仍严格校验。
- 校正后的感知范围随知情一起注入，避免把只看到结果写成目击过程，或把听到的说法当作本人证实的事实。依据不足仍待确认，不用别的楼层倒填，不自动循环重试。
- 兼容有明确含义的空数组遗漏、单层包装和冗余说明空值；不猜测损坏回复，不因一条不合格判断丢弃其它有效校正。日志增加脱敏字段类型和条数，用户详情显示校对说明及对应原文。
- 后台校对沿用后台总结等待时间，不再误用连接测试的较短等待设置。修复内存投影对象共享时可能改变历史快照的问题，原总结和旧批次保持不变。
- 修复思考文本内反引号标签示例被误识别为真实开始标签，导致遗漏前置规划段落的问题；仍保留时间和真正正文，证据偏移保持精确。
- 用实际配置的模型做旧备份与知情反例测试，并验证真实回答到应用保存、重启、选择性重试、知情注入及来源/聊天变化保护；无需删除旧记忆重新总结。模型判断仍可能有遗漏，原文确无依据的条目不会强行标为已解决。

## 0.17.5

- 校对采用源绑定的编号证据：模型选择片段，程序回填逐字原文；兼容旧引用格式。保留相邻获知语境、时间及否定信息，排除规划、变量补丁、脚本和样式，减少无关输入。长段落连续分段，不截掉尾文。预览显示请求字符量和固定调用数。
- 原文明确的无歧义人物简称可用于校对身份绑定，复用已有字典规则；简称不等于知情证明，歧义、来源变化和错误编号仍拒绝。相邻段落可以合成准确原文范围，不再因代词句边界要求模型重新抄写全文。
- 本次实际解决数与模型返回数分开显示、记录。部分有效补正独立保留；未解决项进入“已尝试，仍需确认”，内容和来源未变时不默认重复收费。人工校对、逐条预览和显式重试保留，未确认知情仍不注入，重启和旧存档兼容。
- 未采用建议按条目显示最近结果，不再把历史拒绝累加成不断增长的错误数；接口失败不会抹掉上一次拒绝原因。原内容变化时重新进入待处理，删除后不继续出现在队列。
- 合并重复校对结束刷新，复用本轮风险扫描；待确认列表折叠、按需分页，进度不重建展开的编辑器。增加来源引用、无进展收尾、重启/删除/恢复、部分成功、原附件只读回放及窄屏交互回归。没有自动调用模型重做旧总结。

## 0.17.4

- 校对分为“风险待核对”和折叠的“可选补充”。缺少心迹、参与者未逐人建立知情记录、合理模糊时间等不默认触发 AI；真正缺获知依据、约定状态未知等仍保留保护。修改建议被拒绝不再凭空增加事实错误数量。
- 校对按有效记录计算剩余问题，显示已校对条数及逐条未采用原因。可在条目内人工编辑，或单独预览该条 AI 请求；原总结、楼层和历史批次保留，不需全部重做。
- 小范围问题跨原总结批次共用来源，在用户预算内合成请求；超过预算只在预览阶段拆分，执行时不增加隐藏调用。普通总结与前台召回不增加请求。
- 按模块提供可修改字段清单；不变编号回显不再误伤正文。独立有效修正可在不合格台词或其他独立字段被拒后保留；互相依赖的命题、状态、时间仍一起校验，人工确认与来源保护不放宽。
- 支持只补获知证据、原位解除待核对；精确重复的知情提议用于修正旧条目而不另增副本。原文明示且无歧义的昵称可绑定正式名，未知别称和规划文字不作为证据，不凭相似合并不同命题。
- 超时日志增加是否观察到页面切后台；计时器明显延迟时单独说明，不将墙钟等待冒充模型计算时间。已保存内容保留，恢复前台后可只重试未完成项，不自动重复计费。
- 增加真实备份只读计划回放、字段隔离/误报反例、自动入口零额外调用、来源/人工编辑保护、恢复重试、窄屏运行中交互及升级回归。模拟验收不等于所有模型语义准确或物理手机后台可持续运行。

## 0.17.3

- 人工校对与AI校对明确分开：问题条目下方直接展开原文和修改表单，可确认知情人物、具体内容、渠道、时间、约定状态及DIY字段，不调用模型。分页展示问题，进度更新不重建展开的编辑器。
- AI校对先生成计划，显示预计调用数再开始；仅选待处理问题及失败项，一次读取共同来源。按用户输入预算规划，不再额外写死30K，拆分在预览前完成；超预算单项提示人工处理，执行中不膨胀总数。
- 同组部分校对修正保留，重试只补剩余问题；人工改动只使本条及直接相关补正失效，不撤下同组无关校正。旧批次、原文、人工控制及存档兼容，不要求全部重新总结。
- 向量编码分段让出主线程，SHA256优先使用本地异步实现，旧环境使用同算法分片；删除重复整库序列化和已验证读回后的重复哈希。存档格式、数值精度及精确写后验证不变。
- 增加备份4只读计划回放、固定请求数、人工校对与重启、损坏检测、向量持久化和运行中UI检查。模拟测试不等于所有手机实测或模型语义质量保证。

## 0.17.2

- 约定状态无法识别时单独标为待核对，保留来源和正文；不再因该字段让整批总结失败，也不猜成已接受或已完成。待核对约定不参与自动召回或事件附带约定，现有“内容校对”可单独修正，普通总结不新增请求。
- 中文 GAL 双语台词支持“角色【动作】「日文」〔中文〕”，核对原话及说话人；规划、变量分析和私密内心不能冒充公开台词。
- 人设变化遗漏心迹字段时，优先保留同一角色、同一来源明确的内心独白；否则将已保存的人设描述作为第三人称“阶段观察”，明确不是逐字心声，不杜撰第一人称日记。显式不填、手动编辑及删除保留。
- 推荐预设增加获知者原文依据、转告/亲见/阅读边界、玩笑预测不是约定、双方重要回应及心迹填写示例。无法对应获知者的显式证据暂不注入，原内容保留待核对。旧 DIY 预设只补新增规则，不覆盖用户文本。
- 内容校对支持约定状态、知情渠道/状态、心迹和关键台词的源绑定修正；未解决条目仍能重试，不重跑总结。日志分别记录待核对条数、心迹/阶段观察数、台词过滤数，不导出正文或密钥。
- 增加日志18问题的去标识回归：20/30/50楼一次请求、重启保存、召回隔离、独立校对、来源/覆盖保护、双语原话、旧预设兼容。离线程序测试不等于真实模型语义准确率；升级不自动调用模型重写旧记忆。

## 0.17.1

- 总结、合并、辅助整理和助手按同接口及凭据共用聊天请求队列。新增全局 RPM 配置；默认不假设服务商配额。临时错误渐进冷却，尊重 Retry-After；明确上下文／内容限制等不作无意义重试。向量和重排独立，不新增默认模型调用。
- 排队可取消，取消不向服务发送排队请求；实际接口超时从发送时起算。中文等待提示、队列位置、排队耗时与实际请求耗时分别记录，不导出 Key／接口认证信息。
- 可选校对／合并队列遇到网络、认证、限流或服务故障时保存进度并暂停；不再依次探测所有待处理记录。稍后重试只处理未完成项，单条内容校验失败仍与接口故障区分。
- 原文检索保留分隔较远但与查询有关的限制／取消／知情片段；保留各段及相邻语境，不把关键词当作提取事实。预算不足时不再以丢条件的短摘要替代必要原文，注入日志保留具体省略原因及计数。
- 增加虚拟时钟、跨角色队列、全局配置持久化、停止等待、长聊天早期来源及最终注入回归。真实 Gemini 3 Flash 合成测试与程序测试分开报告；分段阅读实验未证实改进，未纳入默认。没有把本次结果推及 Gemini 3.1 Pro，也不硬编码“64K 注意力”上限。

## 0.17.0

- 记忆页新增“关键对话”和“角色心迹”：同一次总结在现有记录中提取重要原话、回应语境和阶段心迹，不新增默认模型调用。当前总结预设包含填写规则与示例，已有自定义预设不覆盖。
- 台词按来源去重，日记式心迹按人物和相邻阶段归档；支持搜索、分页、条目内编辑、删除、暂停注入与手动补充。编辑保留其他字段和聊天隔离；私密心迹、推测、用户编写与历史阶段明确区分。
- 重要对话可随相关人物本地补入，当前心迹并入完整人物参考，回顾往事时可带历史阶段；不把私密想法赋予其他角色知情，不要求复读台词。新增两个全局注入开关与注入日志统计，原有事件召回和人物档案不截短。
- 支持有明确说话人的跨行原话和无引号独立台词，不把旁白转述当逐字引语。校验丢弃时显示具体提示。
- 502 保留未知、超时、上下文限制和明确内容过滤等区别；上下文/过滤报错不作无意义自动重试。日志补充实际请求 UTF-8 字节数（不是 Token 数），不记录密钥或原始请求正文。普通网关错误不能据此判定为内容限制，也不添加绕过服务限制的提示词。

## 0.16.15

- 新增“记录 → 总结预设”：真实总提示词、九模块规则和独立追加要求可编辑，默认从当前实际请求规则生成；附中文字段示例、真实主总结提示词/规则预览。支持复制、命名、删除、恢复推荐、JSON 导入导出；保存并启用才生效。
- 预设全局持久保存，手动与自动总结共用。新批次冻结当前预设，分工及复核沿用该预设；运行中编辑不改变该批。来源、枚举、输出结构、只读 MVU 权限仍受程序校验，不新增模型调用。只发送当前预设，不把整个预设库塞进总结或助手请求。
- 人物全量知情的明确事件关联可本地补入相关背景速览，旧的笼统“知道这件事”有明确关联时补足指代；同事件只带一次。不回填删除/隐藏事件，不给未获知角色扩散秘密，不将清楚但与本轮无关的知情变成全量历史注入。无关联记录保留原状，不猜事件。
- 记忆详情显示知情所指的事件；召回预览、逐次注入日志记录补入背景、来源与体积。保留原有历史候选预算、人物全量信息和 0.16.14 手机界面性能优化。

## 0.16.14（本地候选，未发布）

- 手机悬浮窗拖动按动画帧合并更新，拖动过程中缓存边界并使用位移；停止拖动后才保存位置。保留键盘避让、旋转边界及误触拦截。
- 后台进度与日志通知不再立即复制整份记忆；界面只读取当前页面需要的数据，收起后仅更新状态提示，不重建内容区。日志记录、后台总结和注入照常执行。
- 记忆未变化时不重建列表；单条变化只替换有变化的卡片，保留其余卡片和控件。只生成当前显示范围内的详情，全部记忆仍可检索、编辑和召回。
- 切换召回/向量页可复用 5 秒内已完成的索引状态检查；手动刷新和记忆变更后的校验不跳过。不更改任何总结、召回、人物全量注入或 API 设置。
- 包含此前日志 17 的待发布修复：大文档 SHA-256 内存优化；更完整且脱敏的服务错误分类、请求字符数及保存耗时记录；严格整楼依据下的中文模块来源备注兼容。未增加模型请求或放宽跨楼来源验证。

## 0.16.13

- 修复重启后“自动注入”设置仍已保存、真实发送却没有带入记忆：自动跟随聊天时恢复已保存的功能开关，不需要重新点击面板启用。
- 用户在本次运行中主动暂停仍有效，切换聊天不会解除暂停；首次安装和已保存的关闭设置不会自动发起注入或总结。
- 增加未手动打开面板的重启发送回归测试，检查真实请求钩子、注入日志一致性、聊天切换及关闭开关，而不只验证召回预览。

## 0.16.12（本地候选，待完整验收）

- 修复单次模式“续跑”反复读取来源编号错误的同一回答：用户主动续跑时，只重新请求来源无效的失败子批，不猜编号、不自动多次重试；保存失败但内容有效的回答仍直接复用。
- 兼容旧版已经保存的无效来源响应；不修改正常请求数量、已有记忆或真实来源校验，也不干扰双次复核的独立恢复。

## 0.16.11（本地候选，待完整验收）

- 消除关系字段提示的歧义：人物属性 from/to 是旧值/新值，人物关系 from/to 是双方人名，变化放在正文；不把“合作伙伴→恋人”当作两个角色。
- 总结明确区分集合时间与预约时间，保留后来收紧的限制，不把旧许可外推到新场景。正常整批调用次数不变；提示改进不是内容永远准确的保证，原文与独立校对仍保留。

## 0.16.10（本地候选，待完整验收）

- 完整九模块单元素外层数组与省略格式标记可同时兼容；仅接受原有严格模块形状，不合并多份回答。来源只多包一层数组时保留所有引用并逐项核对。
- 全量人物档案中的关系、知情、人设与演绎记录补上来源楼层，避免没有日期时新旧状态无法辨别；来源顺序不是猜测的生效日期。
- 明确询问下一步安排时优先保留相关人物尚未完成的已存约定；双主题问题不再因第一条已有原文而跳过另一主题。沿用已有篇幅和条数上限，不增加模型调用。

## 0.16.9（本地候选，待完整验收）

- 修复 JSON 同名空区块覆盖已生成内容：只对明确九模块格式中的重复空数组保留唯一非空结果；不同的非空版本、嵌套重复字段、其他重复键仍拒绝，不替模型拼接或生成内容。

- 修复真实50楼回答在事件来源中加入“回忆部分”等非真实片段备注导致整批失败：必须是唯一未拆分来源，且同一来源有独立整楼摘要定位，才保留同一完整楼层为依据。
- 逐楼摘要、真实分片、来源版本与校验仍严格核对；不跨楼猜测，不增加模型调用。运行日志中文说明事件备注兼容，不冒充原文精确短语匹配。
- 已知关系依据类型误填入事实性质时，保留原文并明确标为“未确认”，不提升为观察事实；其他任意非法值仍失败。
- 增加真实 Git HTTP 更新/重启隔离验收、长30/50楼连续剧情与万条干扰记忆检索基准工具（不随手机插件发布）。

## 0.16.8（本地候选，待完整验收）

- “绰号”与昵称、别称采用相同的有来源字典投影，避免模型换一个常用字段名称就漏掉人物完整档案；用户校正和停用优先级不变。
- 人设范围限制、原文短语定位的兼容警告提供中文说明，区分本地整理与新增模型请求。

## 0.16.7（本地候选，待完整验收）

- 兼容模型误将原文短语当作片段编号的返回：只在唯一、未拆分的指定楼层内精确定位；继续绑定该完整楼层，不生成片段，不更换来源。日志明确记录 resolvedSourceTextHints 警告。
- 不在原文内的编号、重复短语、真正已拆分的来源、楼层歧义及版本/校验信息冲突仍拒绝；不增加修复调用。

## 0.16.6（本地候选，待完整验收）

- 修复中文人名边界碎片误触发原文补充：先排除完整人物称呼，再进行原文主题分词。避免“甲和乙”中的人名碎片让无关楼层抢占注入预算。
- 同步用于重排的原文摘录；不改变人物全量注入、事件预算、模型请求数或既有存储。

## 0.16.5（本地候选，待完整验收）

- 修复已保存昵称因缺失事实性质而不进入检索字典，导致昵称查询未带出完整人物档案的问题。复用现有有来源昵称字段，保留原记录的未确认标记，不新增调用或改写身份。
- 推测、否认、撤回记录以及用户校正/停用仍优先生效；共享昵称仍作歧义处理。保留 0.16.4 的场景限制修复。

## 0.16.4（本地候选，待完整验收）

- 修复真实 Flash 回答漏填人设适用范围导致整批追加修复的问题：缺省时限制为所引场景的观察，并在展示、注入和日志中保留这个限制，不推定长期或普遍人设。
- 不补造对象、来源、知情状态；明确无效字段仍报错。已有范围、期限及故事内容不改写。
- 增加真实手动到自动连续总结的隔离验收；按实际楼层核对，不把未选择的后续楼层计作漏记。旧冻结制品及失败证据保留。

## 0.16.3（本地候选，待完整验收）

- 完整九模块回答的单对象外层数组可无损展开；不接受多候选、截断或错误来源。
- 辅助字段未注明不再导致整批丢弃；知情主体、状态、来源、人物适用范围仍严格校验。不把缺失渠道猜成亲眼目睹。
- 可选复核兼容有明确来源的扁平拆事件结果，保留知情说明及原子提交；不强制每批额外复核。
- 统一记录主总结、复核与局部处理中的无损归位诊断。人物全量注入、DIY、旧记忆和凭据保存规则不变。
- 新版本制品单独构建，不覆盖既有 0.16.2 冻结包。正式发布与实机状态以验收结果为准。

## 未发布工作源码（不同于既有 0.16.2 冻结包）

- 后台总结与复核独立超时，正常候选双次模式保留完整主回答，仅做来源支持的增量复核；失败缓存恢复不重做已返回的主总结，不暗拆10楼。
- 复核原文内联编号、重复提示无损压缩；修改集中一次提交，仍拒绝冲突目标与错误来源。检查说明数量不再冒充事实准确率。
- 楼层原文可追溯、按问题选择有来源段落；逗号不拆断限定问题。相关人物仍完整注入，召回不继承总结长超时。
- 缺来源、保护字段、流式中断和配额耗尽分别诊断；新增未修改真实响应回放与负面质量回归。
- 未完成真实内容质量验收，未发布。最新完整证据与限制见 `reports/CONTINUOUS-FINAL-2026-09-12.md`，本地测试/模拟不等同 Android 真机或模型语义通过。

## 0.16.2（本地候选）

- 无事件关联的知情记录不再被普通召回排除；来源完全一致时随本楼显示，不编造跨事件关联。不适当关联被隔离后也保留独立检索；隐藏、删除与注入开关继续有效。
- 校对同值人物属性时同时比较生效/截止时间、情境与事实性质，保留动态属性回到旧值的历史；兼容旧版嵌套有效期。
- 提取规则明确条件期限、跨天事件区间、约定完成来源与关键台词要求；尚待真实服务复测，不自动修写旧记忆的时间或事实。
- 未修改的真实0.16.1模型回答加入回归：20楼九模块、独立知情和预设12个问题。本地回放不是新的真实模型生成，不把关键词命中当语义全合格。
- 真实LUNA0.16.1一次请求保存成功，但约4分55秒的任务往返时间及内容缺陷仍未满足完整验收；不自动发布。

## 0.16.1（本地候选）

- 单次提取使用扁平九模块传输格式，避免楼层→变化→属性→知情多层嵌套；程序绑定来源、楼号和局部编号，仍拒绝未知来源、混合格式、缺楼和不完整模块。不增加归档模型调用，不迁移用户存档。
- 加强当前时间/回忆时间、秘密获知时点、动态属性复用及人设适用对象的输出规则，减少反复复制背景和状态。
- 校对支持人物属性recordRef；知情去重同时比较对象、状态、渠道和获知时间，保留由不知情到知道的变化。旧校对、用户修改和跨聊天隔离继续有效。
- 新增并复测20/30/50楼全部模块的保存、重开、切换、重生、批量删除恢复和写盘失败续跑；真实模型结果另记，不把模拟结果写为生产验收通过。

## 0.16.0（本地候选，待真实模型复测）

- 单次总结支持逐楼附带变化的紧凑输出，宿主展开为现有存储结构；保留旧格式、分工模式及暂存响应恢复，不另起付费归档请求。
- 人物事实可关联独立知情记录；来源、楼层覆盖、ID 隔离、枚举及已有事实校验继续执行。动态字段及嵌套值不写死为固定属性。
- 兼容嵌套属性有效期，抽取来源明确的昵称；字典手动覆盖、称呼歧义和全量相关人物注入不变。
- 叙述中的转述不再作为逐字台词。可疑知情—事件关联只隔离附带注入，不删除知情记录，注入日志增加隔离计数。
- 收紧字典主题扩展和无当前主题依据的本地召回。有来源的冲突/核实结论参与检索，保留完整限定及核对提示；无来源疑点仍不自动注入。
- 增加冻结真实回答回归、20/30/50 楼单次合成流程、旧存档读取、隐私日志和紧凑格式手机宿主模拟测试。测试报告区分模拟/回放与真实模型结果，未承诺真实总结速度达标。

## 0.15.0

- 自动总结移出 TT 等待的回复事件，后台队列合并重复通知；停止、暂停和切聊天不遗留延迟启动的旧任务。
- 总结与聊天发送分离：模型等待及新批次刷新期间召回上一份完整记忆；纯新增发布不打断正在进行的召回，删除/替换/设置变更仍拒绝过期内容。
- 去掉产品的固定 46,000 输入上限；保留用户预算和旧任务冻结计划，整批优先一次请求。模型无需的来源散列仅留在本地，不删剧情或降低回复上限。
- 读取范围和来源验证只规范化需要的正文；查末楼只读分页信息，最近楼层正在生成不再阻止整理旧楼。
- 日志记录准备、模型等待、发布和实际处理楼数；100ms 内合并写盘，结束/导出强制核对保存。强制退出可能丢失尚未写入的尾部日志。
- 增加 20/30/50 楼跨越旧上限的一次请求、阻塞模型时继续发送、发布竞态、删除/切聊天及后台停止测试。移动回归继续运行官方 TT 2.2 激活/历史代码与隔离原生桥，不冒充 Android 或真实 API 性能验收。

## 0.14.2

- 修复 0.14.1 长批次在模型请求发出前被误报“聊天来源已变化”：原文校验使用总结引擎实际冻结的分段证明，不再按另一套范围重切。真正改楼、切聊天或停止时仍阻止旧结果提交。
- 已失败批次可直接“继续未完成”，包括 0.14.1 留下的请求计划；已提交片段或已返回模型结果按原有检查点复用，不要求删除记忆或重新生成。
- 运行日志区分读取原文失败、聊天切换、正文变化和请求计划不一致，并记录校验范围；不记录 Key、聊天正文或任意服务端错误文本。
- 运行中再次点续跑不另起任务；TT 2.2 首页的已确认无聊天状态不再误报后台错误，其他宿主读取异常仍明确记录。
- 增加应用级长批次、原文保护、保存故障、重启及旧版升级测试；移动回归通过官方 TT 2.2 历史接口执行真实分段路径。自动化使用隔离数据和模拟模型响应，不冒充 Android 实机或真实模型成功率保证。
- 发布流程必须先通过完整测试及移动宿主回归，并核对测试产物校验和；测试后再改运行时代码，必须重新验证。

## 0.14.1

- 总结发送前用实际序列化请求做本地容量检查；整批装得下时一次调用全部区块，超大批次也优先合并可容纳的片段，不再强制按保守正文配额多次请求。
- 不删减所选楼层正文、不降低用户回复上限；模型输入去掉重复存储校验字段，原始来源证明保留在本机校验。
- 冻结实际请求分段，失败、重启后继续原计划，复用已返回结果和已保存片段；旧版本的未完成任务仍沿用旧计划，不因更新错配进度。
- 进度与日志明确显示预计请求数、实际请求序号和恢复重试，向量请求分开记录。
- 手机恢复前台时重新检查请求和重试的截止时间，日志记录实际等待及计时器延迟。无法保证 WebView 被系统挂起或结束后仍在后台执行。
- 保留已有 API、上下限、分工开关、聊天记忆、字典、向量索引与人物全量注入；不增加总结子模型或要求重建索引。

## 0.14.0

- 新安装默认一次主总结；额外分工、逐对合并和分组校对为可选付费调用。旧配置保留，增加“按次计费：减少额外调用”按钮。
- 单次模式同样隔离临时编号、校验原文变化；异常行保留精确字段诊断，失败仍按现有结果续跑。
- 本地过滤明确主题查询中仅同名的无关候选，不影响语义命中、已重排候选或完整人物档案。
- 同事件且同时间/知情边界的注入片段共享重复说明，不改存档、不增加模型请求；注入日志显示压缩数字及过滤原因。
- 批次展示编号按楼层排序连续生成，删除/重试不累加，永久 ID 与连续楼层进度保持不变。
- 更新内置助手说明与合成高楼旧事召回、少调用、归组安全性及批次管理测试。

## 0.13.0

- 默认两阶段总结，支持辅助整理模型独立连接或只更换模型、共用总结地址与 Key；模型列表、连接测试、凭据持久化沿用独立全局设置。
- 成功阶段独立暂存；失败阶段续跑，不重做已成功阶段；聊天或来源变化时不再调用后续阶段。
- 相关上下文按内容选择、去除存储冗余，后台校对限量选取参照并自动拆组，前台任务可抢先运行。
- 精确事件引用纠错；模型局部编号隔离，防止多片段楼层覆盖；续跑保持原拆分计划与已提交凭证。
- 任务缓存清理同步目录并先检查存在性；保留回复上限，不再因超时私自降为 4K。
- 日志新增阶段、逐项输入长度与原有服务端 Token 对照；加强过度关系推断的校对提示。

## 0.12.3

- 总结输入增加独立的移动端安全上限；用户仍可保留较高的显示预算，但单次实际请求不会把旧记忆候选无限塞进模型上下文。
- 外层“总结多少楼”保持不变，超长的 10 楼范围只在内部无损拆成更小请求，逐楼摘要和事件来源仍完整保存。
- 旧版本遗留的超大恢复分片与检查点自动丢弃并按安全分片重新执行，避免恢复任务反复带入 64K 级别的旧请求。

## 0.12.2

- 每个源楼层子批次独立计算恢复与补全次数，避免前一批耗尽整次批量总结的恢复预算。
- 对高输入预算增加移动端源材料安全护栏，避免单次请求膨胀到 8 万以上估算单位；超大单楼仍无损拆分。
- 502/超时后的恢复请求先压缩无关历史上下文，并临时采用本批合理的回复上限；用户设置仍保留为总上限。
- 运行日志新增恢复压缩阶段及前后请求大小、移除数量、实际回复上限，界面明确提示正在压缩后重试。

## 0.12.1

- 修复恢复缓存的可选目录写入失败阻断正式总结的问题；缓存只用于断点续跑，不能覆盖权威总结流程。
- 修复单区块校对常见的非空数组返回；校对改为逐条接受有效证据，失败条目保留待重试，不再整组作废。
- 超时与连接失败最多自动恢复一次，减少 Android 端重复等待；502 等短暂服务错误仍按有限次数恢复。
- 运行日志明确记录缓存警告与真实总结失败，避免把插件内部缓存故障误报成模型总结失败。

## 0.12.0

- 修复中文日期与时分区间解析；相对日期按事件日期换算，当前时间只从当前聊天最近正文读取。多个日期、回忆或无法定位的时间跳转不取最大日期，更不使用全局日期。时钟读取失败不阻断已验证记忆召回。
- 有来源的独立简称、称呼和主题字段补入派生字典；别称歧义、手动校正和删除仍优先，不凭相关主题合并人物。提示词要求检查各模块间遗漏和关系过度解读。
- 增加独立内容校对：原总结先保存，候选缺项或矛盾由总结模型读原文核对。严格绑定原记录、原文版本、证据引句，拒绝跨聊天或修改后迟到的结果；自动开启，可关闭。每组一次请求，失败单独重试，无需重做摘要。
- 校对补齐知情与动态人物属性；人工确认不自动改写；确定不了的住址等作为疑点显示与注入，不猜哪个版本正确。补充项可编辑、删除、恢复；原批次删除/重生使相应校对失效；支持整体撤销校对。
- 内容校对面板显示进度、待处理项；运行日志记录校对请求、校验理由和保存结果。备份同时导出原记录、校对资料和有效记录视图。内置配置规则增加校对指南。
- 人物全量注入不变；在线召回不增加总结或校对请求，向量按现有内容指纹增量更新。Android 实机及真实模型的语义质量仍需用户原文验收，不把合成测试当作保证。

## 0.11.2

- 补齐主总结、逐楼补全、字段纠错和区块补全的请求编号、用途、实际请求上限、返回状态、解析与校验信息；区块补全前保留原始校验字段，并将这些字段传给补全模型，不再只知道“总结响应错误”。
- 区分空正文、空模型内容、HTML/无效外层 JSON、模型 JSON 语法错误、错误根结构、截断、内容过滤、网络中断和超时；保留安全代码位置、错误类型、原因链与服务提供的已识别错误分类，不记录密钥或原始正文。
- 总结、向量、重排、模型列表和助手共用请求诊断入口；补上知识库向量日志缺失、召回降级、MVU 读取、手动编辑/配置/备份操作与本插件未捕获异常的记录。取消后的迟到响应不再记录为成功。
- 日志保留最近 2000 条、最多 2 MB；明确显示已清理数量、任务部分保留情况、旧日志版本未知和日志自身存储失败。导出不受页面筛选/分页影响，每条新日志带执行版本。
- 校验日志保留最多 512 个具体问题，并明确标出超过容量的数量。连续日志合并写入，失败不阻断总结保存，不增加模型调用次数；存储、来源校验、人物全量注入规则不变。
- 服务端未返回的细节、旧版本已丢弃记录与强制退出前尚未写入的日志无法补录；日志不会猜测这些原因。

## 0.11.1

- 相关人物档案全量注入：全部已启用属性、对应关系、人设变化、知情与人物演绎记录，不设人物上限，不占历史记忆条数/长度额度。
- 历史候选池排除已全量带入的档案，避免大量人物属性挤走事件；保留字典、BM25、向量和重排路径，不增加人物筛选模型调用。
- 当前输入和最近 AI 正文参与本地人物匹配；长正文中间的人名及续写场景不再遗漏。字典关联主题不冒充人物明确提及，歧义别称不强行归人。
- 完整保留自定义结构、0/false 数值、不同有效期、适用对象、知情范围、观念与台词；去重不会混淆同名但不同 ID 的 DIY 字段。
- MVU 配置字段读取保留完整文本，向前寻找最近有效快照；外部只读状态不再被四千字裁剪，也不会因没有事件召回而跳过。
- 注入日志区分全量人物与历史条目，真实总数不受明细显示条数限制。内置配置规则升级 1.6；沿用用户开关，不修改旧记录或手机整体布局。

## 0.11.0

- 人物与事实改为动态档案，按人物归拢任意属性，不限定字段名单。相同取值合并显示，各次来源与时间可展开；不同取值保留为变化记录，不自动判定最新楼层就是当前状态。
- 人物只保留一个“修改人物”入口，统一编辑、添加或移除任意属性；文字、JSON、数值修改一并提交，保留来源。整份档案可一次撤下并从回收站恢复；DIY 和 MVU 区块不混入基础档案。
- 关系枚举中文化并展示具体表达、回应及双方确认依据，保持方向；所有基础区块使用各自的详情标题。新总结明确要求补充区块的中文经过和开放属性。
- 知情卡只呈现自身的角色、内容和获知时间，不再连带其他角色的不同知情内容；获知时间不冒充事件发生时间。人物与关系等区块不再附加不适用的“时间/地点未提取”，明确关联事件可独立展示已有时间与地点。
- 保留底层独立证据和所有旧存档。档案归拢不扩大注入；完全相同且时间/知情条件一致的事实在召回中去重。人物编辑不调用模型，无新增依赖。
- 字典校正改为条目内展开并带入别称与关联词，多草稿独立保留；新增和校正分开。召回解释区分 BM25、向量、重排与事件合并，提供24/48候选档位且不扩大最终注入预算。
- 自动总结单独成页，有启用、暂停、检查进度和处理下一批。按真实连续保存的楼层推进，保留最近楼数；不再取最新 N 楼、不用批号乘当前批大小计算进度。起点按聊天保存，其余设置保持全局；失效来源和覆盖缺口不能推进。
- 知识库增加切片预览、分隔符、世界书 JSON 解析、独立关键词/字典/向量选项、逐库启停和继续任务。字典分析按约6000字分组并逐片保存；共享资料向量可在未打开聊天时建立，成功索引复用，失败不删除原文。旧资料启停保留原片段编号；无需重新总结。
- 内置配置规则升级1.5，匹配整体人物编辑、自动楼层进度和知识库的实际操作。新安装自动每批10楼、重排候选24条；不覆盖老用户已保存的参数或 API。
- 手机窄屏与原生桥接模拟回归，真实 Android 端到端验收仍待完成。
- 修复 TT 移动端禁用缩放导致复选框取消后仍显示勾号；勾选显示改用透明度，开关状态与实际值一致。

## 0.10.0

- 总结批次新增单批、所选及全部未完成的续跑入口，与重新生成分开。同一任务复用冻结输入、已返回结果、已提交子批；正文或规则变化则停止复用。
- 完整模型响应先暂存，提交失败可在重启后继续校验与保存。缺失楼层和符合条件的补充区块只局部补全；临时错误与补全共享每批最多两次额外请求。取消、认证、TLS、来源冲突和长期限流不盲目重试。
- 所有小数存档采用可兼容旧数据的无损文本封装；不放宽比对精度。写入回执丢失时用精确读回确认。未写成功的检查点不进入成功缓存；已确认提交但辅助回执失败时，从不可变记录恢复，不重做模型请求。
- 向量增加独立响应检查点，覆盖“服务已返回、进度文件写失败”的恢复；按来源散列匹配，不能把旧向量套在修改后的文字上。完整索引再次点击继续时，不重复请求或改写检查点。
- 日志新增暂存、复用、补全、恢复次数和等待时间；内置配置规则 1.3 同步真实能力。原始响应不进日志、召回或普通备份；暂存受大小、数量和时间限制。
- 保留原 API、全局设置、各聊天记忆和旧索引；不添加 v1、不强制 Key、不改真实模型参数。没有按区块分配多个 API 或把完整索引改为分片存储。Android 实机和真实供应商端到端验收仍待完成。

## 0.9.2

- 向量存档改用带完整性校验的 JSON 文本封装，避免原生 JSON 桥接的小数末位变化触发逐字读回失败；不量化、不放宽校验。旧数字索引和中断检查点仍可读取，后续写入自动升级。
- 已成功获取并保存到检查点的向量，在索引发布失败后可直接续存；“重试全部未完成”不重做总结，跳过成功索引。存储失败提示不再被刷新覆盖为待处理。
- 日志区分本机写入、读取、读回、内容比对和完整性校验，区分续传检查点、正式索引和重建暂存，不记录正文、向量值、Key 或路径。
- 总结遗漏人设变化有效期时兼容为“期限未确认”，不再仅因此拒绝整批；人物、对象、情境、范围和来源校验保留。演绎注入明确适用边界，不能据期限未知推定永久变化；有依据的期限原样保留。
- 增加 1024 维小数向量、原生桥接小数漂移、旧索引兼容、中断续存与窄屏重试回归。Android 实机与真实供应商端到端验收仍待完成。

## 0.9.1

- 向量页新增“重试全部未完成”、单条重试、失败筛选和逐条中文错误。直接使用当前聊天已保存的记忆补建，成功项自动跳过，不调用总结或合并模型；失败状态与分段进度重启后保留。
- 长记忆完整分段编码，按片段与总输入长度分批，不截断原始纪要。召回复用一次查询向量，匹配各片段并取最高相似度，不在聊天时重建索引。
- 明确请求 float 向量；数量/序号不匹配或服务拒绝一整组输入时，有界拆小请求，单输入缺失序号可无歧义兼容。无效向量不会写进索引。认证、限流、网络或网关错误停止队列，避免连续消耗请求；成功检查点保留。
- 全量重建使用独立暂存索引，完成前保留旧的完整向量空间，中断后继续暂存进度，避免混合不同维度。删除、修改记忆或切换聊天不会把旧结果写入新聊天。
- 运行日志新增向量筛选、输入条数/字符数、返回数量、维度、保存计数、HTTP 与网络错误。WebView 拒绝请求会注明直连通道，无法凭此区分 TLS、网络和跨域；不记录 Key、端点、正文或原始响应。
- TT 2.2 原生向量后端尚未实现，本插件向量仍为 WebView 直连，不宣称升级即可解决所有供应商网络限制。无需重做已成功的总结。

## 0.9.0

- 总结与跨批事件合并解耦：先校验并保存来源有效的总结，再用总结模型逐对核对合并建议。缺失目标、时间问题、502 或格式错误不再把已保存总结判为失败；逐楼完整性和来源校验仍保留。
- 新增“记录 → 事件合并”：分页查看待办/结果、只重试合并、搜索改选目标、保持独立与撤销。默认总结后自动处理新候选，每次最多 10 对；每对一次附加请求，使用总结 API 的预算与认证，可关闭，不无限重试。
- 合并为派生视图，不覆盖原始事件或批次。绑定事件内容和当前批次归属；删除、重生、编辑后旧投票失效，避免幽灵段落或旧合并复活。知情和时间按来源片段保留，不能扩散为全员知道整件事。
- 合并失败有独立运行日志与 HTTP 状态；不会重做总结。召回、注入和打开聊天不调用合并模型。已有字典、向量索引仍随有效记忆更新。
- 兼容旧的待合并记录；成功解决后不再显示旧版自动生成的核对提示。配置规则升级为 1.1，新增自动合并开关进入全部可配置字段清单。

## 0.8.0

- 注入日志按聊天保存：记录实际加入待发送请求的片段、查询、来源楼层、字典/标签命中、去重与篇幅取舍、各阶段耗时。保留最近 30 次（大记录可能提前淘汰），支持分页、删除、清空及 TT 原生导出。默认导出不含剧情，不记录 Key 或完整请求；“已加入”不表示服务端已收到。
- 总结与知识库分析产生自动字典；增加已验证参与人物、地点名称的确定性补充。字典支持删除与恢复，删除标记阻止下一次自动生成重新出现；校正和停用继续可用。
- 开启向量后默认允许后台增量更新（旧设置保留）；向量列表支持排除/恢复、打开原记忆修改。排除只影响语义索引，不删除正文或关键词检索，手动重建按钮继续保留。
- 内置 8 组版本化配置规则，助手可按需读取、查看全部非密钥设置的推荐值及当前值、检查索引和最近注入的脱敏诊断。新手按钮与导入规则入口分开；不冒充未实现的多总结 API 路由或破坏性工具。
- 新用户默认 5 楼一批、输入预算 24000、总结回复上限 8192，字典/标签/分类召回开启；旧值不覆盖。设置中可确认后应用推荐参数，不改 API、Key、记录偏好或自动运行开关。向量、重排与自动总结仍需用户开启。
- 修复时间表述无法确认一致时整批总结失败：不强行合并，而是将有来源的新事件独立保存、附核对说明并继续保存其它模块；明确时间冲突、缺失目标、来源与结构错误仍拦截。正常可确认事件仍沿用原合并流程，不增加模型请求。
- 未默认拆分多个总结 API：保留同批跨模块关联、一次提取和原子保存，先用较小分批与相关旧记忆输入控制负担。无法凭此承诺真实模型的召回质量或固定秒数。

## 0.7.1

- 修复完整总结因单个知情状态写法不合规而直接整批失败的问题：先兼容明确等价写法，再对仍不合规的枚举字段最多自动纠错一次。
- 纠错使用同一总结 API，只发送问题记录和关联来源，严格限制可修改路径及枚举值；必须引用该记录已有来源，不能修改时间、正文、来源或其它字段。未知不默认映射为已知/不知情，纠错后重新完整校验才保存。
- 正常结果不追加模型请求；纠错保留原预算及取消限制，失败不无限重试，不替换旧成功记忆。运行栏和日志增加中文纠错阶段及字段数，知情状态错误使用可读名称。
- 不读取或补传旧的用户日志正文；回归使用合成记录，真实供应商对该次错误的原始枚举值仍不可从脱敏日志确认。

## 0.7.0

- 新增独立“召回”入口：概览、字典匹配测试、向量索引进度及分页列表、策略、注入设置和本轮结果。区分 API 配置、功能开关、索引完整度与实际调用状态。
- 分类检索在候选阶段执行，与关键词、标签和向量候选合并后只重排一次；向量分类候选复用同一次查询向量与余弦计算。
- 修复标签候选预留把高分命中挤到队尾的问题；中文与数字混排保留完整编号，避免把 50 当作 500 的一部分。新增一键推荐分类策略，不改动用户 API 或注入上限。
- 当前查询可参考最近一条剧情回复，补足代词；不读取系统提示词作为场景。重排使用速览与相关证据，旧的无速览记录不擅自截断。
- 注入不再因预算充足而展开完整纪要；按当前查询补充相关句段，保持时间、知情和后续条件。确切关联且内容与约束均已覆盖的事件/楼层记录不重复注入，保留原始纪要与逐楼摘要。
- 启用向量后可勾选后台增量更新索引，默认不勾选；会使用已配置的向量 API，只处理新增或改变的记录。未启用向量不自动联网，失败保留已保存部分且不无限重试，切换聊天停止旧任务。
- 手机端缩小分类块和卡片留白，保留可点击面积；记忆列表显示两行速览，完整经过与操作按需展开。API 配置仍是独立入口。

## 0.6.3

- 批次默认列表排除已删除项，新增“当前批次 / 回收站”入口与独立计数；单批、批量删除后立即移出，恢复后回到当前列表。切换筛选清除选择，避免误操作隐藏项。
- 楼层总结明确提取人物、时间、地点及事件链接；旧楼层可通过精确正文来源关联已有事件元数据。不同事件的知情范围分开展示，不倒推后续获知，不把参与者等同知情者。
- 修复缺少字段就显示“原文未明确”的误导文案；详情和召回均携带可关联信息，保留具体时分，不把旧记录当作新增事实写回。
- 合并校验识别等价日期及精度补充，保留已有的更精确时间，修复同批连续合并的临时 ID 链。目标缺失、真实时间冲突、无法确认的时间表述分开记录诊断，不泄露原始回复。
- 保留已保存记忆、来源校验、回收站与聊天隔离。不自动调用模型修复历史，不增加召回请求；本版经过合成数据和手机尺寸联调，非 Android 实机验收。

## 0.6.2

- 打开、切换、重启后自动绑定当前聊天并加载独立的记忆和总结批次；关闭悬浮窗、暂停自动功能仍跟随聊天。返回首页时清空旧聊天视图，再次进入自动恢复。
- 切换事件不阻塞 TT 自身加载，快速连续切换只保留最新目标；旧任务取消并保留来源校验，迟到响应和旧批次读取不会覆盖新聊天。正常切换取消改为普通提示，不再要求反复重新打开聊天。
- 编辑、删除、重 Roll 后自动重新核对来源，过期内容不继续召回；既有批次目录保留，可删除或重生。悬浮窗重新打开时也会核对当前聊天，补偿遗漏的宿主事件。
- 全局 API、Key、通用设置、资料库和配置助手历史不随聊天切换重置。不新增轮询、后台模型请求、依赖或存档迁移。

## 0.6.1

- 相同起止楼层只保留一个批次入口，普通手动总结与重新生成使用相同的替换机制：新结果完整保存后切换，失败或停止保留上次成功结果。重生成时不把待替换结果当增量上下文。
- 打开聊天时整理旧版完全同范围的重复批次，优先保留最近成功结果，旧待处理占位不覆盖成功记录。旧批次目录留本地恢复副本，原始记忆历史不物理删除；部分重叠但不相同的范围不擅自删除。
- 批次管理支持多选、选择本页、选择全部筛选结果、批量删除/恢复及按楼层顺序重生。批量删除可恢复，聊天原文不变。
- 总结明确同一事件跨楼续写与既有事件关联规则；宿主检查目标、发生时间与引用，合并完整经过及来源、人物、标签，不再把同一件事的明确后续当独立卡片。相同来源的完全重复内容去重；仅类型或语义相似不冒险合并。
- 记录并展示人物观念/态度及关键台词，区分说话人、对象、语境和含义；引语核对原文，知情范围独立保留。支持搜索，不要求聊天模型复读台词，不增加召回时的模型请求。
- 旧摘要缺失的观念、台词及语义关联不会凭空补写：在“记录 → 批次管理”选择旧批次后重生即可按新规则整理。API、Key 和全局设置保持不变。

## 0.6.0

- 总结默认要求简体中文完整事件纪要，保留原因、参与者、时间地点、行动对话、转折结果、取消与未完成事项；逐楼摘要和人物等模块不能代替事件经过。同一次请求生成召回速览，不额外调用总结模型。
- 记忆列表显示标题和楼层来源；展开阅读完整纪要与中文知情信息，批次详情同样显示完整经过。旧记录不凭空补写；可在原批次重新生成。
- 自动字典来自当前聊天总结及已启用资料库分析，记录有来源的名称、别称和中文标签；同名别称标记歧义，不擅自合并人物。词条支持校正、停用、撤销校正、关联检索词及分页查找。
- 默认开启标签候选辅助，在全局 BM25 / 语义候选之外补充本轮相关标签候选，去重后最多重排一次。一次查询向量复用于全部标签，不增加逐标签模型请求。
- 检索保留完整纪要；注入先装入速览，再按余量展开完整经过或与问题相关的连续句段。知情、时间和后续约束一起计算预算。手改正文会清除旧速览，标签可修改。
- API、设置、Key、聊天原文与已保存批次不迁移、不清空。旧向量如与新索引文本不符会标记过期，需要用户重建；未重建部分仍可关键词召回。

## 0.5.5

- 修正模型提示词对空数组的笼统描述，明确九类记忆数组与 coverage 对象及子字段类型，补全来源引用规则和关系变化选项。不改变用户的输出上限。
- 结构及来源校验增加区块/记录/字段定位，区分类型、无效来源、来源歧义、版本或片段不一致、必填字段与枚举等问题。运行提示和日志均可查看。
- 在来源归一化前诊断非法记录与非法引用，不再将其静默丢弃或误报为网络问题；合法来源字符串和别名继续支持，既有来源真实性与完整保存检查不放宽。
- 字段日志采用固定词表和长度上限，不包含动态来源 ID、哈希值、聊天内容、模型正文、地址或 Key；支持重载、导出及旧日志兼容。旧请求缺失的字段不能事后恢复。
- 不增加自动重试或模型调用，不迁移、不删除现有设置和记忆。

## 0.5.4

- 新增“记录 → 运行日志”和底部日志按钮，最近 200 条本机持久化，支持任务阶段/楼层详情、筛选、分页、导出与清空；不收集 Key、地址、正文和原始模型回复。
- 总结日志区分请求、响应、逐楼摘要检查、内容校验与保存，显示实际回复上限、结束原因、服务用量和缺失/重复/空摘要楼层。
- 补全模型侧逐楼摘要字段和约定；先复用现有来源绑定再检查逐楼完整性，修复合法来源别名或字符串格式被误报为缺楼的问题，不放宽来源真实性或完整保存要求。
- 将逐楼摘要缺失与模型明确截断、服务拦截、HTTP 错误分开提示。不自动增加回复上限，不自动重复调用模型，不生成缺失的虚假摘要。
- 保留原有记忆、批次、设置及 Key；新日志记录在独立全局存储，日志故障不会让已保存记忆变成失败。

## 0.5.3

- 修复 TT 2.2 读取尚未建立的拾忆检查点、总结批次等记录时，宿主先弹出红色“Chat store entry not found”、随后插件才捕获错误的问题。
- 聊天存储优先使用原生可选读取；只有 getJson 的 TT 聊天接口先用 listKeys 查询自身命名空间，再读取存在的记录。不缓存不存在状态，不预建空文件，不扫描其他聊天。
- 权限错误、损坏文件、异常目录响应及读取期间文件消失仍作为失败处理；保存继续读回校验。已有记忆、批次、全局配置和 Key 不迁移、不删除。
- 增加缺记录原生通知回归测试；移动端测试改用官方 v2.2.0 的真实聊天句柄代码，覆盖存储和历史分页，底层磁盘与模型响应仍使用隔离模拟数据。

## 0.5.2

- 修复 TT 已打开聊天，但拾忆仍要求先点“开始使用”才能手动总结：新任务自动确认并按需读取当前聊天，恢复对应存储，保留 API 和 Key。
- 手动范围改为“最近 N 楼 / 指定起止楼层”二选一，隐藏禁用未选择的输入；分批楼数独立，开始前显示选择，读到正文后显示实际范围与楼数。
- 修复总结请求和新逐楼摘要把楼层从每批 0 重新编号的问题，保留宿主的真实绝对楼层号。旧存档不自动重写。
- 强化加载、读取、保存前的聊天身份校验；读取途中切换对话会停止，不将旧请求带入新聊天。空聊天、未就绪和读取失败分别提示。
- 手动总结不自动启用后台任务；原“开始使用 / 暂停”更名为“启用自动 / 暂停自动”。默认总结楼数明确标为初始值。

## 0.5.1

- 修复保存 API 不保存 Key 的问题：四类模型 Key 使用独立本机存储并读回确认，重启自动恢复；输入框不回显，提供保存状态与清除按钮，允许无 Key 接口。
- Key 不进入配置助手消息或拾忆导出，按服务来源绑定，改成不同服务地址不自动发送旧 Key。记忆持久化与认证凭据继续分离。
- 故事记忆入口统一字号/高度，同级正文排版统一。“待核对”改为“冲突与疑点”并解释处理方式，仍不参与自动召回。
- 新增记忆下拉框包含扩展模块及字段；MVU 区块只读，从该入口可以刷新变量。
- 记录页分离总结与批次管理。默认 10 批分页，支持翻页、跳页、楼层/编号/侧重点与状态过滤，展开时才渲染详情，保留删除/恢复/重生。
- 旧版已经丢失的 Key 需要重新填写一次。保存位置为 TT 本机数据，不声称是加密钥匙串；请保管好整机 / TT 备份。

## 0.5.0

- 新增默认折叠“扩展模块”，用户可自行新增多字段区块、编辑、归档与恢复；不占用原九类入口。
- 配置助手支持读取区块定义、探测当前 MVU 路径、提出新增 / 修改 / 删除方案，确认后全局保存，支持撤销与冲突检查；兼容无工具调用接口的 JSON 方案。
- 自定义区块支持随总结记录、手动记录、MVU 只读三种来源。总结区块复用同一次模型请求、来源校验和批次管理；MVU 字段不能被总结模型改写。
- MVU 数值按聊天保存带楼层来源的快照与变化，路径缺失不猜值，失败不以旧值充当当前状态；同楼刷新不重复建记录。
- 支持区块定义 JSON 导入 / 导出；纯配置和全局备份含定义，聊天备份另含快照。默认不参与召回，用户可启用后共用已有预算。
- 测试覆盖配置持久化、聊天隔离、助手确认、数值类型与只读保护、实际发布 UI；Android 实机及真实 MVU 卡仍需用户环境验证。

## 0.4.0

- 所有非密钥配置改为全局保存；助手对话、方案、文件资料与全局备份脱离聊天绑定。故事记忆仍隔离，旧档保留并按首次明确打开的聊天迁移。
- 手动范围按指定楼数分批，新增持久批次编号、范围、九类产出、楼层摘要、删除 / 恢复 / 重生操作。重生失败保留旧结果，未完成结果不参与召回。
- 九个记忆区块始终显示，包括零条状态；支持分类手动新增、修改、删除、回收站恢复。
- 总结与助手分别配置输入预算和回复上限，回复上限真正传入 max_tokens；0 沿用服务默认值。修复旧记录增多时可选上下文挤占请求结构空间造成的预算超限。
- 保留手机悬浮、键盘避让、固定结果提示；未宣称 Android 实机或真实模型质量已通过。


## 0.3.9

- 修复总结 / 助手连接测试只有 16 Token、将截断回复误判为连接失败的问题；短测试改为 512 Token，不修改正式总结预算，不自动追加请求。
- 分开显示“已收到完整文本”“服务已响应但回复受限”“请求失败”。达到输出上限、空文本、仅思考字段、服务拦截等为黄色提示，不冒充完整生成已通过。
- 认证 / HTTP 错误、超时和无效响应仍作为错误显示；识别 TT HTTP 200 包裹的错误，不把它当成测试成功。
- “地址与认证”改成“高级连接选项（通常不用改）”，解释“地址如何使用”和“Key 发送方式”。保持原设置值，不自动补 `/v1`，不强制要求非空 Key。
- 正式总结的截断校验不放宽，仍不能把不完整结果当作成功记忆保存。

## 0.3.8

- 修复拉取模型、测试连接、保存 API 错误要求“聊天来源有效”的问题。API 操作不再打开、读取或绑定当前聊天，也不需要先点“开始使用”。
- 总结、助手、向量、重排的连接配置改存 TT 全局扩展存储；同一 TT 的所有聊天共用。API 页的请求超时与助手输入预算也属全局，故事记录和召回偏好仍按聊天保存。
- 切换聊天、编辑正文或重 Roll 不再取消模型列表 / 测试连接；更改 API 表单仍使旧列表失效，主动停止仍可取消请求。
- API 地址、模型与认证选项重载后自动恢复，无需打开聊天；Key 仍仅在内存保留，切换聊天不清空，重载后需重填。
- 首次打开旧聊天可继承旧版连接配置，但不会覆盖已保存的全局配置；不读取其他聊天，不迁移或混合记忆。
- 保留 0.3.7 的键盘避让与固定操作反馈；保存配置做读回校验，失败不伪报成功。

## 0.3.7

- 修复 Android 输入时重复扣除键盘高度、悬浮窗内容裁切的问题；使用 TT 稳定视口和当前窗口的键盘指标，移除重复避让空白。
- 悬浮窗采用单一表单滚动区；标题、导航和底部结果提示固定可见，输入聚焦只滚动内容，不把标题或保存按钮推走。
- 四类模型列表的标准 `/models` 请求改走 TT 原生网络，不再受 WebView 跨域限制；保留自定义完整地址、不自动补 `/v1`、允许空 Key、不读取主聊天密钥。
- 拉取列表、连接测试显示进行中及结果；识别 TT 用 HTTP 200 包装的错误，不把认证 / 网络失败当作成功的空列表。失败保留手动填写的模型。
- 手动与自动总结增加统一状态反馈，只有完成校验与保存后才报成功；区分 HTTP 错误、超时、无效输出与主动停止。窗口底部提供任务停止按钮，错误提示不再随表单滚动消失。
- 回归测试加载官方 TT 2.2.0 完整样式、移动几何规则和输入焦点路由，覆盖键盘覆盖 / 缩小视口两种模式。仍不代表 Android 实机或任意真实 API 已完成验收。

## 0.3.6

- 修复实际悬浮窗首次打开未填充设置的问题；推荐数值立即显示，打开当前聊天后读取原有配置，不用默认值覆盖用户设置。
- 实际运行界面调整为“记忆 / 记录 / 助手 / API / 模块”；注入、检索、世界资料分开，数据与备份保留独立入口。保留可拖动悬浮按钮与窗口。
- 新配置默认向量与重排地址为 `https://api.siliconflow.cn/v1`，模型分别为 `BAAI/bge-m3`、`BAAI/bge-reranker-v2-m3`。旧配置可点击“补齐推荐值”补齐空白，非空自定义配置不替换。向量、重排仍需按需启用；Key 不预填。
- 总结、独立助手、向量、重排各自支持拉取模型列表、下拉选择和手动输入。拉取与测试使用表单中的当前地址，不要求先保存或先填模型；不会自动补 `/v1`。
- 助手默认沿用总结模型，折叠无需重复填写的独立连接。模型列表失败时可继续手动输入；完整自定义端点可单独填写列表地址（仅本次会话）。
- 数字字段空白会提示，不再把空白静默当作 0 保存。更改地址、认证或切换聊天时使旧模型列表失效，迟到结果不覆盖新选择。
- 修复部分移动 WebView 点击悬浮按钮后，兼容点击落在新窗口按钮上造成误跳页的问题；新的点击和键盘操作不受影响。
- 此次是实际界面与配置修复，不代表 0.4 规划中的自动字典、楼层分批重构、完整 MVU 和人物分析模块已经交付。

## 0.3.5

- 聊天界面默认显示“拾忆”悬浮按钮，可触摸或鼠标拖动；点击直接打开独立悬浮窗，不必先打开扩展页面。
- 悬浮窗标题栏也可拖动；窗口为非模态，不锁住聊天界面。收起保留当前页面、输入与后台任务。
- 按钮和窗口位置仅保存在当前设备，旋转屏幕或键盘变化时保持可达。方向键也能移动按钮和窗口。
- 扩展页只保留快捷入口与悬浮按钮显示开关；即使隐藏按钮，仍可从快捷入口打开。
- 修复未设置总超时保护时，计时延迟可能提前跳过重排的问题；各接口时限真正独立执行。
- 沿用 0.3.4 的召回引擎和数据格式；本次不包含开发中的 0.4 全面界面重排。

## 0.3.4

- 复用当前聊天的关键词索引与向量缓存；记忆变化时更新，减少每次发送前的重复构建和读取。切聊天、修改、删除及配置变化会使对应缓存失效。
- 检索候选数量与最终注入条数分开，默认候选 24 条，最终注入上限不变；不通过缩短历史范围、关闭向量或重排来提速。
- 不设强制三秒召回总限。新增可选在线总超时保护，默认沿用各接口时限；新配置的向量和重排时限各为 4000ms，已有设置保留。
- 在线接口失败或向量索引不完整时，明确提示召回降级。保留有效本地结果，不把降级当成完整召回；迟到结果不能恢复已隐藏或删除的记忆。
- 导入 JSON 时先检查格式，避免把无效 JSON 当作成功导入。
- 198 项自动化测试通过，包括 90 组关键词排序和 40 组向量排序对照。真实模型的语义准确率及 Android 实机端到端体验仍需验证。

本次为召回引擎与可靠性更新，不包含开发中的 0.4 新界面、自动字典或新版人物分析模块；不改变现有数据存储格式，不清理记忆与助手历史。

## 0.3.3

- 修复 TT 2.2.0 被提示“需要 ST 2.2.0、当前 1.18.0”而无法加载：移除误用的 ST 最低客户端版本声明。
- TT 应用版本不再填入 ST 前端版本字段；聊天、历史与存储接口仍保留运行时能力检查。
- 增加官方 TT 2.2.0 扩展加载器的回归验证，覆盖“旧清单被拒绝、新清单能够激活”的路径。

## 0.3.2

- 固定 Git 发布文件的原始字节，避免客户端自动转换换行导致校验清单不一致。功能与数据格式不变。

## 0.3.1

- 修复手机工作台滚动后“收起工作台”遮住导航按钮的问题。
- 调整滚动避让，保留键盘上方输入和发送的可达性。

## 0.3.0

- 增加公开 Git 安装渠道，开启 TT 扩展更新标记。后续更新仍用同一个仓库。
- 工作台接入 TT 移动端安全区域与键盘避让契约；窄屏下调整按钮、表单及可滚动范围。
- Android 备份与配置导出改用 TT 原生文件导出，失败不再悄悄退回可能无效的下载链接。
- 增加版本与更新说明，版本号与发布清单统一校验。
- 沿用聊天独立存储，不迁移、不清理现有记忆。API Key 仍仅保存于会话内存。

Android 实机端到端验收尚未完成；不宣称所有手机或模型 API 均已验证。

## 0.2.0

- 总结、本地召回、知情与时间参考、可选向量与重排。
- 自然语言配置助手、对话历史、MD / SKILL 分段分析及方案应用。
- 官方 Windows TT 2.2.0 隔离环境验证保存、请求注入和暂停。
